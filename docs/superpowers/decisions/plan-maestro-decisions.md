# Registro de decisiones del Plan Maestro

Actualizado: 2026-09-05. Este registro resume decisiones aprobadas; el detalle operativo está en el maestro. Cambiar una decisión exige fecha, responsable, impacto y entregas afectadas.

| ID | Decisión vigente | Impacto principal |
| --- | --- | --- |
| PM-001 | Documentación local en `/opt/fusionbikes/herramientas` es canónica; MCP es espejo eventual. | Continuidad E0–E24 |
| PM-002 | Programa renumerado E0–E24; una entrega solo termina en estado `aceptada`. | Gobierno |
| PM-003 | Zona operativa `America/Argentina/Buenos_Aires`, formato 24 h y fecha explícita. | Jornada, SLA, auditoría |
| PM-004 | ML tiene prioridad absoluta. Web tiene máximo normal de preparación 15:00 con calendario de excepciones. El transporte único retira ML, Andreani y Flex; ML/Andreani deben estar listos con 30 min de margen y Flex debe salir como máximo a las 17:00 para permitir el regreso antes del cierre de las 19:00. | E1, E4 |
| PM-005 | La ola inicial conserva sus necesidades normales; pedidos normales posteriores forman mini-olas y ML urgente puede incorporarse a la ola activa. La reasignación automática de la última unidad queda en E12. | E1, E12 |
| PM-029 | **Superada por PM-031 el 2026-09-04.** UM1 urgente separa Guardia ML, Corrección y Consulta. La cobertura válida es publicación+variación con SKU Woo exacto; decisiones históricas sin SKU no cubren. La primera publicación es solo lectura y no escribe ML. | UM1, E1, E11, E12 |
| PM-030 | **Superada parcialmente por PM-032 el 2026-09-04:** cae la confirmación de stock compartido; el resto sigue vigente. Un SKU puede publicar su stock completo en varias claves ML con confirmación de stock compartido. No se promete cero sobreventa: el exceso agregado abre incidente crítico y retiene pedidos según plazo ML, antigüedad y prioridad ML sobre web. | UM1, E11, E12 |
| PM-006 | La misma persona puede preparar, fotografiar y aprobar; despacho es otra responsabilidad. | Permisos y auditoría |
| PM-007 | Una foto cuenta solo tras guardado, validación y procesamiento del servidor. Evidencia se retiene 180 días salvo hold. | E2 |
| PM-008 | Aprobación, auditoría y encolado de etiqueta interna son atómicos; un job idempotente por paquete. | E2, E3 |
| PM-009 | Etiqueta interna 50×25 y etiquetas de transporte son artefactos y momentos distintos. | E3, E4 |
| PM-010 | Woo es autoridad del disponible comercial; Fusion modela físico, comprometido, no disponible, condicionado y entrante sin doble descuento. | E8–E12 |
| PM-011 | Stock físico se deriva de movimientos inmutables; correcciones son inversos o deltas auditados y nunca generan saldo negativo. | E9, E10 |
| PM-012 | Rollout de stock por familia/SKU con familia, ubicación, conteo base y reconciliación; no hay retorno a escrituras legacy. | E9–E12 |
| PM-013 | Recepción es de dos pasos: entrada y putaway; Woo aumenta solo al ubicar disponible. | E14, E15 |
| PM-014 | Conteo inicial ciego con snapshot y reconciliación; tolerancia versionada y alto riesgo con control reforzado. | E16, E17 |
| PM-015 | App es herramienta de piso, iPhone primero; Android queda fuera hasta demanda concreta. | E5, E13–E21 |
| PM-016 | Offline es captura provisional cifrada, ordenada y con lease; jamás usa `last-write-wins`. | E13, E15, E17, E21 |
| PM-017 | Inbox durable distingue reconocer de resolver y enruta por área/turno. | E6, E7 |
| PM-018 | Cancelaciones, devoluciones, daños, proveedor, garantías y taller cambian stock por tareas y movimientos auditados. | E18–E21 |
| PM-019 | Métricas requieren un mes de línea base y no publican rankings personales. | E22 |
| PM-020 | Backend/web pueden publicarse por pipeline verde con rollback automático; Windows y App Store requieren autorización explícita. | E3, E5, E23 |
| PM-021 | Cada entrega dura idealmente 3–5 días, usa flag, piloto acotado y una jornada observada. | Todas |
| PM-022 | App y backend fijan `/api/v1` por commit, con compatibilidad corta y actualización obligatoria cuando corresponda. | E5, E13 |
| PM-023 | E1 usa tablero PC, celular web centrado en una tarea y tablet futura como tablero compartido sin PII; App iPhone futura tendrá el flujo completo de piso. | E1, E5, E13 |
| PM-024 | Un operario mantiene una sola tarea abierta. La ayuda se solicita por zona manual, identifica al ayudante al recibir productos y no permite que el ayudante modifique cantidades, pedidos o estados. | E1 |
| PM-025 | La búsqueda consolidada no escanea unidad por unidad. La confirmación unitaria ocurre en mesa; la herramienta sugiere prioridad y el responsable confirma la asignación. | E1 |
| PM-026 | La ola tiene estados `disponible → en búsqueda → en mesa → cerrada`; el pedido tiene estados independientes y una ola cierra solo con unidades asignadas, devueltas, resguardadas o derivadas explícitamente. | E1 |
| PM-027 | Errores relevantes, reintentos agrupados y correcciones se auditan con actor, hora, antes/después y motivo; los eventos no se borran. | E1, E2 |
| PM-028 | Sustitución transitoria puede autorizarla el operario con constancia del cliente y motivo; Fusion actualiza el pedido comercial, ML bloquea si no puede reflejarlo y un flag de Admin habilita la transición futura a Ventas. | E1 |
| PM-031 | UM1 deja de ser «Guardia ML» y pasa a ser el programa de Identidad de productos UM1.1–UM1.6: Matcher, Cobertura y Guardia se **reemplazan** por un núcleo bilateral ML↔Woo con `Producto Fusion` como identidad canónica y `fusion_sku = FB-{id_woo}` no editable. Supera PM-029. La unidad es `item_id + variation_id` y solo cubre con verificación remota de menos de 60 minutos o excepción explícita `solo_ml`. | UM1, E1, E11, E12 |
| PM-032 | Compartir un SKU entre publicaciones es la operación **normal**, no una excepción: 511 SKUs ya comparten 1176 publicaciones. Cada clave vinculada publica el stock Woo completo y no se pide confirmación de stock compartido. Supera esa parte de PM-030; sigue vigente que no se promete cero sobreventa y que el exceso agregado abre incidente crítico. | UM1, E11, E12 |
| PM-033 | `seller_custom_field` es evidencia auxiliar y **nunca** cobertura: solo `SELLER_SKU` tiene semántica de identidad. `skuDesdeAtributosMl` dejó de usarlo como fallback. Verificado contra las 21 pruebas de `test/guardia-ml.test.js`: el cambio no rompe Guardia. | UM1 |
| PM-034 | En esta base `user_version` **no** numera migraciones: es la compuerta de la migración Hito 7 (`user_version < 30`, al final de `openDb`). Ninguna migración nueva puede escribirlo. Subirlo saltea Hito 7, deja la base sin `device_tokens` y tira toda la auth móvil. La idempotencia de cada migración la da su marcador en `_schema_migrations`. `test/identidad-productos.test.js` afirma `user_version === 30` y la presencia de `device_tokens` para que no se rompa en silencio otra vez. | Esquema, E5, E13, UM1 |
| PM-035 | El trabajo sin commitear de un agente que se queda sin contexto se rescata en un commit propio, atribuido a él y declarado explícitamente como no verificado, antes de tocarlo. Separa lo recibido de lo corregido y permite medir la regresión contra una base limpia en un worktree aparte. | Gobierno, todas |
| PM-036 | La pantalla de UM1.1 se construye **acotada a UM1.1** (salud, conciliación, cola ML→Fusion, detalle del caso, operaciones, historial) y no espera a la herramienta unificada de UM1.5. Motivo: el gate 6 de UM1.1 exige E2E 390/768/1440 y sin superficie no hay nada que probar. `Productos Fusion` queda como lista de solo lectura hasta UM1.3, y la cola Woo→ML hasta UM1.4. | UM1.1, UM1.3–UM1.5 |
| PM-037 | Lista+detalle solo por encima de 850 px. En 390 **y 768** el caso ocupa la pantalla completa con botón de volver: a 768 la comparación ML↔Woo en dos columnas queda ilegible. Fija la ambigüedad del plan, que decía «PC lista+detalle, móvil pantalla completa» sin definir la tablet, y mantiene el mismo umbral que el resto de las pantallas de Herramientas. | UM1.1, UM1.5 |
| PM-038 | El modo (`shadow`/`enforced`) se muestra siempre en pantalla y el detalle avisa explícitamente que no se escribe en ML. No es un detalle interno: decide qué puede hacer la persona, y ocultarlo llevaría a creer que una decisión ya tuvo efecto remoto. | UM1.1, UM1.6 |
| PM-039 | La conciliación del gate 2 se muestra como **resultado de la igualdad** (`total = verificadas + excepciones + urgentes`), no solo como cuatro números sueltos, para que un desvío no pase inadvertido a simple vista. | UM1.1 |
| PM-040 | El vínculo manual se hace con **búsqueda humana explícita** por nombre, SKU Woo o GTIN, con vista previa obligatoria (clave ML, SELLER_SKU actual → objetivo, producto y stock a publicar) antes de confirmar. El candidato aproximado por familia, con puntaje y explicación, es UM1.4 y no se adelanta. Solo se ofrecen productos `activo`: un provisional no tiene `fusion_sku` y no puede sincronizarse. | UM1.1, UM1.4 |
| PM-041 | Los archivos con CRLF y BOM del repo (`public/home/index.html`, entre otros) se editan preservando esos terminadores. Una escritura que los normaliza convierte un cambio de 16 líneas en un diff de 2547 y vuelve la revisión imposible, escondiendo cualquier cambio real en el ruido. | Gobierno, frontend |
| PM-042 | El E2E de una pantalla incluye axe-core sobre la pantalla real en cada ancho y falla ante violaciones `critical` o `serious`. Correr accesibilidad aparte de la pantalla levantada es trabajo duplicado y se saltea cuando el gate aprieta. | UM1.1, frontend |
| PM-043 | La igualdad del gate 2 (`total = verificadas + excepciones + urgentes`) **no alcanza** como evidencia: se cumple igual de bien con la clasificación 100% equivocada. Medido sobre el universo real el 2026-09-04: daba `conciliado: true` con las 1201 claves activas con stock marcadas `sku_ausente`, cuando 907 tienen SKU exacto. El gate exige además cero observaciones incompletas. | UM1.1, UM1.2 |
| PM-044 | Una clave ML cuya última observación es anterior a la migración 082 (`atributos_json IS NULL`) **no se clasifica**: no genera caso, se cuenta como `observacion_incompleta`, impide declarar el universo conciliado y degrada la salud. Motivo: `seller_sku_presente` tiene `DEFAULT 0` y las filas viejas lo conservan, así que clasificarlas fabricaría un backlog falso del tamaño del universo entero. Solo un refresco ML completo con `include_attributes=all` las vuelve clasificables. | UM1.1, UM1.2 |
| PM-045 | Prohibido rellenar `seller_sku_presente = 1` a partir de un `seller_sku` no vacío del cache viejo. Hasta PM-033 el código usaba `seller_custom_field` como fallback de `seller_sku`, así que ese relleno marcaría como atributo presente un valor del campo legacy — exactamente lo que el plan prohíbe tratar como cobertura. La única vía válida es releer de ML. | UM1.1 |
| PM-046 | La conciliación del gate 2 se calcula en **una sola función** (`conciliacionIdentidad`), usada por la auditoría y por `/resumen`, y siempre sobre el mismo universo: claves activas con stock y con observación completa. Un caso de una clave que después se pausó o quedó en cero deja de contar; si contara, la igualdad quedaría inalcanzable para siempre. Además exige `total > 0`: con el universo vacío la igualdad se cumple sola y la pantalla anunciaría «conciliado» sin haber mirado nada. | UM1.1, UM1.2 |
| PM-047 | `test/invariantes-esquema.test.js` blinda invariantes estructurales de todo el repo, no de una feature: (a) ningún `INSERT` declara distinta cantidad de valores que de columnas —261 analizados, la clase de bug que rompió UM1.1 y que la suite no detectaba—; (b) ninguna migración escribe `user_version` por encima de 30 (PM-034); (c) ninguna ruta, `lib/` ni `server.js` pasa `allowRemoteWrites: true`, así que `shadow` no depende solo de una fila de config; (d) la 082 nace en `shadow`, con escrituras remotas en 0 y la fila 1 creada. Cada guard fue validado reintroduciendo su bug a propósito y confirmando que falla con archivo y línea. Un guard que no se probó contra su propio bug no blinda nada. | Gobierno, esquema, UM1 |
| PM-048 | Hoy **ningún** código de producción invoca `procesarPasoOperacionIdentidad`: no hay worker que ejecute la saga remota. `shadow` está garantizado por estructura, no solo por bandera. Cuando UM1.6 agregue el ejecutor, hay que revisar el guard (c) de PM-047 y decidir dónde vive la autorización, en vez de relajar el test. | UM1.1, UM1.6 |
| PM-049 | Todo escaneo o bootstrap que recorra miles de filas va envuelto en **una** `db.transaction()`. La base usa `journal_mode=delete`, así que cada statement suelto hace su propio `fsync`, y `better-sqlite3` es **síncrono**: un cron pesado no sólo tarda, deja al proceso entero sin responder HTTP. Medido el 2026-09-04 sobre la base real: la auditoría de identidad pasó de **158–200 s a 1,9 s** (~100×) con resultados idénticos. Antes del arreglo, producción quedaba bloqueada ~3 minutos de cada 15. | UM1, rendimiento, operación |
| PM-050 | Pendiente evaluado y no adoptado: pasar la base a `journal_mode=WAL` evitaría que un escritor bloquee a los lectores. No se hizo en caliente porque afecta a toda la aplicación y merece su propia ventana y verificación. La transacción única (PM-049) resuelve el bloqueo observado; WAL queda como mejora de fondo. | Operación, E23 |
| PM-051 | Las transacciones largas de escaneo usan `.immediate()`. Una transacción `DEFERRED` toma SHARED en el primer `SELECT` y recién intenta promover a RESERVED al escribir; SQLite **no puede reintentar esa promoción con seguridad**, así que ahí el `busy_timeout` no ayuda y un lector concurrente abortaría el escaneo completo. Con `.immediate()` el lock se pide en el `BEGIN`, donde el timeout sí aplica. Corrección de dato: `busy_timeout` **no** es 0 — vale 5000 ms, que es el default de `better-sqlite3`, verificado sobre la base productiva. | UM1, operación |
| PM-052 | La transacción única redujo el bloqueo de ~3 minutos a ~2 segundos, **no a cero**: `better-sqlite3` es síncrono y durante el commit el event loop de Node sigue detenido. Medido en producción el 2026-09-04 19:21:46: la app respondió en 1918 ms durante la escritura del escaneo. Un timeout esporádico de ~2 s cada 15 minutos es el comportamiento esperado, no una falla. | Operación, E23 |
| PM-053 | Las miniaturas de MercadoLibre se sirven normalizadas a `https://`. En la base productiva **las 1113 son `http://`** y la herramienta se sirve por HTTPS: el navegador las bloquea por contenido mixto y la pantalla queda con puros códigos, sin nada que comparar. `mlstatic` responde igual por https. Las de Woo ya vienen https. | UM1, frontend |
| PM-054 | El detalle de un caso no se arma solo con la instantánea de evidencia: esa guarda identificadores, no cómo se ve el producto. `obtenerCasoIdentidad` entrega además la publicación ML (título, foto, permalink) y el producto Woo candidato (nombre, foto, SKU, stock), y la cola lleva miniatura. Una pantalla de comparación sin foto ni nombre no permite decidir, que es justamente lo que UM1.1 le pide a la persona. | UM1.1, UM1.5 |
| PM-055 | Al abrir el vínculo, la búsqueda arranca **precargada con el título de la publicación ML** y se ejecuta sola. Sigue siendo búsqueda humana editable, no un candidato automático con puntaje (eso es UM1.4): sin la precarga, la persona tiene que adivinar qué escribir y no tiene con qué comparar. Verificado con datos reales: el caso «Casco Rembrandt Para Niños» trae 5 candidatos Woo con foto sin que nadie escriba nada. | UM1.1, UM1.4 |
| PM-056 | La cola de trabajo lista **sólo casos accionables** (`urgente`, `tomado`, `intervencion`). Un `verificado`/`resuelto` no es tarea, y un `pendiente` ya tiene su operación encolada. Mezclarlos mostraba 1203 filas para 110 de trabajo real y, como el orden es por severidad y no por estado, un caso recién decidido quedaba en el mismo lugar: la persona concluía que decidir no guardaba nada. Nada se oculta: la cabecera informa «N por resolver · N esperando operación · N ya verificadas» y los `pendiente` viven en Operaciones. | UM1.1, UM1.5 |
| PM-057 | Toda decisión confirma en pantalla qué quedó registrado y avanza al siguiente caso. En modo `shadow` el efecto remoto no existe, así que sin confirmación explícita la única señal visible sería... ninguna. El aviso dice además que la operación quedó encolada sin escribir en MercadoLibre. | UM1.1 |
| PM-058 | La pantalla usa el ancho completo, sin tope de 1440px, y el ancho extra se gasta en poner **ML y Woo lado a lado** desde 1200px — la comparación que la persona tiene que hacer— en vez de estirar una columna de texto. La columna derecha apila el panel Woo y el buscador en un flex propio, para que el buscador no arranque recién donde termina el panel de ML y quede un hueco muerto. | UM1.1, UM1.5 |
| PM-059 | Esta pantalla es el andamio de UM1.1, **no** la interfaz definitiva: esa es UM1.5 y sigue pendiente. Lo que falta no es estética sino trabajo de flujo: decidir de a lotes, atajos de teclado para resolver en serie, agrupar por publicación en vez de por clave suelta, ordenar por riesgo real (venta retenida antes que stock 1) y filtros por clasificación. Antes de escribir UM1.5 hay que pasar por `disenador-ux`, con la operación real de los 110 casos como insumo. | UM1.1, UM1.5 |
| PM-060 | El buscador de Producto Fusion usa `norm` y `tsr` de `lib/matcherEngine.js`, el motor que ya existía en el repo, en vez de `LIKE '%texto%'`. El LIKE exigía substring contiguo y era sensible a acentos: medido contra la base productiva, «Casco Rembrandt Para Ninos» sin tilde, «casco ninos» y «rembrandt casco» devolvían **0 resultados**. Se exige que todos los tokens estén presentes en cualquier orden; los identificadores (`fusion_sku`, `sku_woo`, `gtin`) se comparan literales en minúscula, sin normalizar, porque `norm` rompería un SKU con guiones. | UM1.1, UM1.4 |
| PM-061 | `tsr` **satura en 1.0** cuando los tokens de la consulta son subconjunto exacto del nombre, sin importar cuántas palabras extra traiga el candidato: para «cubierta 29» empataban todas y el orden caía al alfabeto. Se desempata por palabras sobrantes (menos sobrantes, coincidencia más ajustada), lo que pone el producto base antes que sus variantes. `tsr` sólo **ordena**: no se muestra porcentaje, que es UM1.4 y exige calibración de 200 casos. | UM1.1, UM1.4 |
| PM-062 | La búsqueda trae todos los productos activos a memoria y puntúa en JS: 5140 filas, 38–55 ms en caliente (100 ms en frío) con memo de normalización. Como `better-sqlite3` es síncrono, ese tiempo es bloqueo real del proceso. El costo crece lineal con el catálogo. **Umbral de revisión: si `productos_fusion` activos supera ~15000, hace falta prefiltro SQL o columna normalizada indexada**; hasta ahí la simplicidad gana. | UM1.1, rendimiento |
| PM-063 | Una consulta que normaliza a vacío (sólo puntuación: «...», «---») se trata igual que una búsqueda vacía. Sin ese corte, `tokens` quedaba vacío, `every` era verdadero por vacuidad y devolvía **todo el catálogo** con un orden distinto al de la búsqueda vacía. | UM1.1 |
| PM-064 | El buscador recibe el **GTIN y el SELLER_SKU de la publicación** y los usa para marcar y priorizar candidatos: coincidencia de GTIN pesa más que la de SKU y que el nombre, porque el GTIN es del fabricante y no depende de cómo esté cargado el catálogo. Cuando SKU y GTIN apuntan a productos distintos, la pantalla lo declara como **conflicto de identidad** y nombra los dos productos. Caso real que lo motivó: «Caja Pedalera Shimano Bb-un300 123mm» tenía `SELLER_SKU FB-28299` pero su GTIN coincidía exacto con `FB-19167`; la pantalla mostraba cuatro candidatos de nombre idéntico sin ninguna señal. Hay 17 casos `gtin_contradictorio`. | UM1.1, UM1.4 |
| PM-065 | La búsqueda usa dos pasadas y **no oculta candidatos**: los que cumplen todos los tokens o coinciden por identificador van primero, los parciales quedan abajo marcados. Exigir todos los tokens daba **cero resultados** cuando el buscador arranca precargado con el título de ML, porque ese título trae palabras que el catálogo Woo no tiene («Bb-un300 123mm» contra «Caja Pedalera Shimano Un300»), y obligaba a la persona a adivinar una consulta más corta. | UM1.1 |
| PM-066 | Cuando varios candidatos comparten nombre normalizado, la pantalla lo dice y aclara que **es un problema del catálogo Woo, no una decisión de la pantalla**. Verificado: los 4 «Caja Pedalera Shimano Un300» tienen nombre idéntico y `atributos_json` vacío — la medida que los distingue existe sólo en el título de ML. Hay grupos de hasta 4 productos Woo con el mismo nombre. Ninguna mejora de buscador puede inventar una distinción que el catálogo no tiene. | UM1.1, UM1.3, catálogo |
| PM-067 | Los candidatos muestran los **atributos de la variación Woo** (`atributos_json` → «Talle: M · Color: Azul»). Es lo que distingue dos variaciones del mismo producto, y sin eso la persona ve una lista de nombres idénticos. Cobertura real: **3080 de 3086 variaciones** tienen atributos cargados; sólo 6 no. La pantalla los ignoraba por completo. | UM1.1, UM1.3 |
| PM-068 | Un producto Woo `variable` es el **padre** de una familia: no tiene stock propio, no es unidad vendible y **no puede ser destino de un vínculo ML**. Se muestra marcado «producto padre — no enlazable», sin botón de elegir. `bootstrapProductosFusion` creó 576 Producto Fusion desde padres `variable`, contra el invariante del plan («un Producto Fusion representa una unidad vendible»): queda como corrección pendiente de UM1.3, no se toca en caliente. | UM1.1, UM1.3 |
| PM-069 | Si el GTIN de la publicación coincide con un producto **padre**, eso **no es un conflicto**: dice que la publicación pertenece a esa familia y que la respuesta es una de sus variaciones, que se marcan «de la familia del GTIN». Sólo hay conflicto real cuando SKU y GTIN apuntan a **unidades vendibles distintas**. Corrige un diagnóstico previo erróneo sobre el caso «Caja Pedalera Shimano Bb-un300 123mm», donde SKU y GTIN en realidad concordaban a nivel familia. | UM1.1, UM1.4 |
| PM-070 | El detalle muestra los **atributos de ambos lados**: los de la publicación ML (`atributos_json`, filtrando ruido logístico e impositivo: `SELLER_PACKAGE_*`, `IMPORT_DUTY`, `VALUE_ADDED_TAX`, `ITEM_CONDITION`, y los que ya tienen campo propio) y los de la variación Woo. Sin eso la pantalla decía «coincide» sin mostrar en qué. Caso real: la publicación de la Caja Pedalera declara `Largo del eje: 12.2 cm` y `Modelo: BB-UN300 110 MM`, dato que estaba disponible y no se mostraba. | UM1.1, UM1.4 |
| PM-071 | Cuando un SKU Woo **no tiene atributos cargados**, la pantalla lo dice explícitamente en vez de dejar el hueco en blanco, y aclara que si comparte nombre con otras variaciones es una corrección de catálogo. Un vacío silencioso se lee como «no hay diferencia», que es lo contrario de lo que pasa. | UM1.1, catálogo |
| PM-072 | `normalizarProductoWc` captura los atributos de productos `simple` y `variable`, que **descartaba siempre** (`atributos: []` fijo). Medido antes del arreglo: 0 de 1478 simples y 0 de 576 padres tenían atributos guardados, contra 3080 de 3086 variaciones — porque sólo `normalizarVariacionWc` los leía. La forma de origen difiere: un producto trae `options` (lista, se une con `', '`) y una variación `option` (uno solo); ambas se normalizan a `{name, option}`. Los valores vacíos o nulos se filtran antes de unir. **Requiere un refresco completo de catálogo Woo para poblarse**: el cambio no reescribe lo ya guardado. | UM1.1, UM1.3, catálogo |
| PM-073 | `99999` en `available_quantity` es el **centinela de stock ilimitado/atípico** de MercadoLibre, no una cantidad: se muestra como «ilimitado / atípico» con el valor crudo entre paréntesis. Mostrarlo tal cual hacía creer que había 99.999 unidades. Hoy son **72 publicaciones**. La pantalla vieja de Guardia ya lo trataba así; la nueva lo había perdido. | UM1.1 |
| PM-074 | El bloque de atributos ML sólo se muestra si queda **algo útil después de filtrar**. Con `UNIT_MEASURE`, `SALE_FORMAT`, `WARRANTY_*` y demás constantes de catálogo/logística en el filtro, una publicación puede quedarse sin atributos útiles: ahí va el mensaje explícito, nunca un encabezado con nada debajo. | UM1.1 |
| PM-075 | El detalle avisa que si MercadoLibre responde «Este artículo no existe» al abrir el enlace, la publicación se dio de baja **después** de la última lectura y el caso quedó huérfano. El cache puede decir `status=active` mientras la publicación ya no existe; la herramienta no puede detectarlo sin una lectura remota autenticada, así que lo declara en vez de dejar a la persona pensando que el dato está mal. | UM1.1, UM1.2 |
| PM-076 | Una publicación cuyo `channels` no incluye `marketplace` (típicamente `["mp-merchants","mp-link"]`) es un **link de pago de Mercado Pago**: existe en la API, figura `active` y suele traer `available_quantity` 99999, pero **nunca estuvo publicada en el marketplace** y no se encuentra buscándola en MercadoLibre. Se captura en `ml_publicaciones_cache.canales_json` (migración 083) y se marca en cola y detalle. Verificado contra la API de ML: 20 de 20 con stock 99999 no son marketplace; 20 de 20 con stock normal sí lo son. | UM1.1, UM1.2 |
| PM-077 | **Superada por PM-085 el 2026-09-05.** La medición detectó que 68 de 85 casos urgentes eran links de pago y dejó pendiente decidir su alcance. PM-085 los excluyó posteriormente del universo UM1. | UM1.1, gate 2 |
| PM-078 | Verificado contra la API de WooCommerce (lectura directa, no cache): las variaciones `FB-28297/98/99` devuelven `attributes: []`, `name` y `description` vacíos y ningún `meta_data` con la medida; el padre `19167` sólo define `Marca` y `Tipo de Artículo`, **ambos con `variation: false`**. Es un producto variable con 3 variaciones y **ningún atributo variable**: la diferencia de largo **no existe en Woo**, sólo en el título de ML. Cuando un candidato no tiene atributos, la pantalla dice que el GTIN es lo único que lo distingue. | UM1.1, catálogo |
| PM-079 | Un refresco completo de ML tarda ~6 minutos y **un `pm2 restart` lo mata en vuelo**. El 2026-09-04 hubo 31 reinicios por despliegues sucesivos y el cache quedó clavado casi dos horas en la lectura de las 20:51, degradando la salud de UM1 sin que nada fallara. Después de desplegar hay que dejar pasar al menos un ciclo completo antes del siguiente reinicio. | Operación, UM1.2 |
| PM-080 | El detalle triangula con **otras publicaciones de ML de la misma familia**, que resuelven lo que el catálogo Woo no distingue: las que reclaman el mismo `SELLER_SKU` (delatan duplicados o medidas mal asignadas) y las que llevan el GTIN del producto candidato (identifican la medida real de ese SKU). Validado en un caso real: el GTIN de `FB-28298` aparecía en dos publicaciones de «117,5 mm», y al corregir el catálogo Woo quedó efectivamente en `Largo: 117,5`. La predicción por GTIN acertó antes de que el dato existiera en Woo. | UM1.1, UM1.4 |
| PM-081 | Un producto Woo `variable` cuyas variaciones no tienen atributos **no se puede comprar en la tienda**: WooCommerce marca el botón `disabled wc-variation-selection-needed` y la tabla de variaciones queda vacía, así que no hay nada que seleccionar. Verificado en `fusionbikes.com.ar` sobre `FB-19167`. No es sólo un problema de identidad ML: es venta web caída con stock disponible. Detectable localmente: padres `variable` con variaciones y cero `atributos_json`. | Catálogo, ventas |
| PM-082 | El sistema **no escribe atributos de producto en WooCommerce**: las únicas escrituras son a `/orders` y un `patch` de `stock_quantity` en recepciones. Ante la sospecha de que el sync hubiera borrado un atributo, se verificó y se descartó. | Gobierno, catálogo |
| PM-083 | **La huella de evidencia representa la identidad, no la observación.** Incluía `observado_en` y el stock; como el refresco borra y reescribe todo el cache con timestamp nuevo, la huella cambiaba en **cada escaneo**, `upsertCaso` lo tomaba como cambio de identidad y **devolvía a `urgente` todo caso ya decidido, cada 15 minutos**. Reportado por el usuario: «volvieron a aparecerme las 109 publicaciones y varias ya tenían decisiones». Las decisiones nunca se perdieron (quedaron en `identidad_decisiones`), pero el estado sí. La huella se versiona (`v2:`) y una huella vieja se reescribe **sin** tratarse como cambio, para que el propio arreglo no cause un último reseteo masivo. | UM1.1 |
| PM-084 | Un caso `pendiente` (decisión tomada, operación encolada) **nunca vuelve a `urgente`**. Si la identidad cambió de verdad, va a `intervencion`: exige una persona y conserva la decisión. La protección anterior sólo existía en la rama del `else`, así que cualquier cambio la saltaba. | UM1.1 |
| PM-085 | Autorizado por el usuario: los links de pago de Mercado Pago **salen del universo de UM1**. `FILTRO_MARKETPLACE` es la definición única y la aplican la auditoría y la conciliación. Un caso cuya clave salió del universo se cierra como `resuelto` en vez de quedar urgente para siempre. Medido: la cola accionable pasó de **109 a 37**, con 72 casos cerrados y la conciliación exacta (1088 = 1051 + 0 + 37). | UM1.1, gate 2 |
| PM-086 | **Superada parcialmente por PM-111 el 2026-09-05.** Estableció el ejecutor durable y sus tres compuertas fail-closed. Sigue vigente esa autorización; PM-111 reemplaza la saga y frecuencia originales por sobrescritura directa y worker cada minuto. | UM1.1, UM1.6 |
| PM-087 | El guard de PM-047(c) **no se relajó** al agregar el ejecutor: el worker pasa `allowRemoteWrites: habilitado`, un valor calculado desde `identidad_config`, no un literal `true`. El invariante «nadie escribe `allowRemoteWrites: true`» sigue verde y ahora significa «la autorización siempre sale de la configuración». | Blindaje, UM1.6 |
| PM-088 | **Superada por PM-102 el 2026-09-05.** Interpretaba los quince minutos como antigüedad del registro y llevaba operaciones nunca intentadas a intervención. PM-102 fija que el umbral mide un intento iniciado sin progreso. | UM1.1, UM1.6 |
| PM-089 | **Si ML ya tiene el SKU objetivo, no se escribe nada.** La saga arranca releyendo: si el `SELLER_SKU` remoto ya coincide, se salta a `activate` (si el stock también coincide) o directo a `restore` (si sólo falta el stock), **sin poner el stock en 0**. Correr la saga completa bajaba a cero una publicación viva durante varias corridas para terminar escribiendo exactamente lo mismo. Medido: **22 de 43 operaciones encoladas ya tenían el SKU correcto**, una de ellas con stock 5. Planteado por el usuario: «¿es muy necesario que dejemos en cero los que ya tienen el sku bien?». La verificación remota se mantiene: se releé siempre, sólo se evitan las escrituras inútiles. | UM1.1, UM1.6 |
| PM-090 | `reintentarOperacionIdentidad` también reinicia `iniciada_en`. El umbral de intervención (15 min) mide contra ese campo, así que sin esto **ninguna operación de más de 15 minutos podía reintentarse jamás**: rebotaba a `intervencion` en el mismo instante. El presupuesto de 15 minutos es **por intento**, no por vida del registro. | UM1.1 |
| PM-091 | El contrato del adaptador usa `observed_at` (inglés) para la marca de lectura, no `observado_en`: lo lee `confirmarLectura` y el nombre es parte del contrato. El primer adaptador escrito usaba el nombre en español y **ningún paso de la saga habría verificado nunca** contra ML real. Lo detectó la simulación con adaptador falso antes de tocar producción. | UM1.1 |
| PM-092 | **Tener varias publicaciones ML para el mismo producto es normal y deliberado**: el negocio ofrece distintas opciones de compra sobre el mismo SKU. La pantalla las lista como **contexto**, nunca como duplicado ni error. Corrige el texto anterior, que las declaraba «un duplicado o una medida mal asignada» — lo señaló el usuario. Es la misma regla que PM-032 del lado de Woo, aplicada al lado de ML. Sí vale mirarlas cuando sus títulos declaran medidas o variantes distintas entre sí. | UM1.1, UM1.4 |
| PM-093 | El stock y la hora de observación se muestran desde la **publicación vigente**, no desde la instantánea de evidencia: al quitarle a la huella los campos volátiles (PM-083) la evidencia dejó de tenerlos y la pantalla mostraba «sin dato». Una instantánea de identidad no sirve para mostrar magnitudes que cambian. | UM1.1 |
| PM-094 | **Primer canario ejecutado en producción (2026-09-04 23:30–23:40)**: `MLA1320264343`, SKU `FB-5965` ya correcto, stock 5. La saga leyó ML, aplicó el atajo, y completó `zero → activate → reprocess` con el caso en `verificado` y **cero escrituras**. Valida de punta a punta configuración → worker → adaptador → lectura autenticada → saga → auditoría por paso. La publicación quedó intacta. | UM1.1, UM1.6 |
| PM-095 | **Poner el stock en 0 no es necesario para cambiar el SKU y tiene costo propio.** Verificado: `escribirSkuEnMl` de `lib/matcherPush.js` sobrescribe `SELLER_SKU` con un solo PUT y sin tocar stock, en producción desde hace meses. Y el cero sí cuesta: ML pausa la publicación con `sub_status: out_of_stock` — hay **4611 así** en la base. El cero existía para cubrir la ventana sin SKU que abre el paso de *limpiar*; sobrescribiendo directo esa ventana no existe. Camino directo implementado y probado (1 escritura, stock intacto); al momento de esta decisión aún no estaba desplegado. | UM1.1, UM1.6 |
| PM-096 | Una operación `completada` protege la identidad ya aplicada frente a una reclasificación posterior `gtin_contradictorio` cuando ML conserva exactamente el mismo `fusion_sku` y el caso sigue ligado al mismo Producto Fusion. El caso permanece `verificado`; la contradicción queda visible y auditada como `gtin_contradictorio_post_verificacion`, sin repetir la escritura. La protección es estrecha: si el `SELLER_SKU` cambia o desaparece, el caso vuelve a urgente. Resuelve la reaparición del canario 1 (`MLA1320264343`). | UM1.1 |
| PM-097 | Si la identidad cambia mientras un caso está `pendiente` pero su última operación sigue `shadow`, con cero intentos y sin pasos remotos, esa operación queda inmovilizada como `obsoleta_por_cambio_identidad_antes_de_efecto_remoto`; no admite reintento. El caso libera responsable y vuelve a `urgente` para una decisión nueva. Una operación que sí empezó permanece en intervención porque puede tener efectos parciales. El canario puede contener hasta dos claves ML explícitas, separadas por coma, y procesa como máximo dos operaciones por corrida; no designa por sí mismo la segunda clave. | UM1.1, UM1.6 |
| PM-098 | **Canario directo de dos publicaciones completado en producción (2026-09-05 01:00 UTC).** Se ejecutaron únicamente `MLA798189569|` (operación 22, `FB-10376`, stock 1) y `MLA1541702013|` (operación 13, `FB-50396`, stock 6), con `lote_max=2`. Ambas terminaron `completada`, `sin_cero=1`, y la relectura autenticada de ML confirmó el SKU objetivo y el stock intacto. No se tocó una tercera publicación. Backup previo: `data/fusion.sqlite.bak-um11-canario2-20260905-010000`. | UM1.1, UM1.6 |
| PM-099 | Tras observar el canario de dos publicaciones, se retiró la restricción y se habilitó el procesamiento general controlado: `modo='enforced'`, `escrituras_remotas_habilitadas=1`, `canario_ml_key=''` y `lote_max=2`. El primer tick tomó dos operaciones antiguas y las llevó a `intervencion` por el umbral de 15 minutos, sin fallo remoto. Backups: `data/fusion.sqlite.bak-um11-retirar-canario-20260905T010521Z` y `data/fusion.sqlite.bak-um11-habilitar-general-20260905T010721Z`. | UM1.1, UM1.6 |
| PM-100 | **Superada parcialmente por PM-104 y PM-112 el 2026-09-05.** Sigue vigente la entrada durable HMAC y relectura puntual de `product.created|updated|deleted`. PM-104 define protección remota tras confirmar una baja/cambio y PM-112 cambia el respaldo completo Woo a 15 minutos. | UM1.2 |
| PM-101 | Un webhook puntual Woo **no puede ejecutar la auditoría global de identidad con `lecturaConfiable=false`**. La primera entrega lo hizo y degradó 1056 SKU exactos ajenos a `stock_no_verificado`, llenando nuevamente la cola. La auditoría global queda reservada al scan ML completo confiable; la reconciliación puntual por producto se implementará separadamente. | UM1.1, UM1.2 |
| PM-102 | El umbral de intervención (15 min) mide **un intento que no progresa, no la antigüedad del registro**. Con `intentos = 0` la operación nunca corrió: descartarla por vieja mandaba a intervención trabajo que el sistema jamás intentó y obligaba a un reintento manual por script. Pasó con **51 operaciones** que quedaron encoladas mientras las escrituras estaban en `shadow`. Una operación con decisión tomada tiene que poder ejecutarse sola cuando le toque. | UM1.1 |
| PM-103 | El impacto sobre variaciones hermanas se confirma **desde la pantalla**, no por script. La operación frena cuando la publicación tiene otras variaciones activas porque escribir el SKU puede afectarlas, pero hasta ahora esa confirmación sólo existía en el momento de decidir: una operación que quedaba `bloqueada_impacto` no tenía salida desde la herramienta. El botón muestra **cuántas hermanas hay y sobre qué publicación** antes de pedir la confirmación —confirmar a ciegas vacía el freno—, pide motivo, queda auditado y es idempotente por `operation_id`. Sólo Administración. | UM1.1, UM1.5 |
| PM-104 | Una baja, papelera o alteración del SKU Woo se confirma releyendo Woo. Si persiste, todas las claves ML vinculadas quedan en stock cero y bloqueadas y se retienen los pedidos afectados. La misma unidad recuperada con SKU canónico restaura stock y libera pedidos sólo después de verificar ambos canales; otro `id_woo` exige transferencia administrativa. | UM1.2, UM1.3 |
| PM-105 | `solo_ml` excluye sincronización de stock Fusion y deja la cantidad bajo administración manual en ML. Sólo Administración puede aprobarla, con motivo y vigencia fechada o indefinida; cualquier cambio de identidad la invalida. Las exclusiones Woo→ML usan la misma autoridad y auditoría. | UM1.1, UM1.4 |
| PM-106 | Son unidades Woo activas los simples y variaciones `publish|private`; padres `variable` no son vendibles. Stock cero conserva Producto Fusion e identidades. Una unidad recreada con otro ID no hereda identidad automáticamente. | UM1.3 |
| PM-107 | “Pendientes” cuenta sólo acciones humanas; operaciones esperando worker tienen contador separado. `bloqueada_impacto` aparece en Pendientes y Operaciones. Una contradicción EAN/UPC/GTIN posterior a una verificación crea tarea de catálogo sin reabrir identidad ni repetir SKU. | UM1.1, UM1.5 |
| PM-108 | Los claims son opcionales; cualquier decisor puede relevar con motivo y `expected_version` resuelve concurrencia. Alertas críticas llegan a Administración y usuarios activos con `matcher:write`. Excepciones, exclusiones, transferencias, familias y modo operativo son sólo administrativos. | UM1.2, UM1.5 |
| PM-109 | Offline por hasta 12 horas permite vínculos y notas, pero no excepciones, transferencias, impacto ni configuración. Un conflicto de versión/huella bloquea sólo esa operación; las independientes continúan y una decisión válida entra al flujo remoto normal. | UM1.5 |
| PM-110 | Producto Fusion es autoridad de códigos y admite EAN-8/13, UPC-A y GTIN-14 tipados, con máximo un EAN y un UPC activos por unidad y unicidad global. UPC-A y EAN-13 con cero inicial son equivalentes para matching. Sólo códigos/productos activos confirman; Woo proyecta el principal y ML conserva todos los observados. | UM1.3, UM1.4 |
| PM-111 | La corrección de SKU relee Woo, usa su stock fresco y sobrescribe directamente `SELLER_SKU` sin modificar cantidad. Un rechazo o verificación fallida pasa a intervención sin fallback automático con stock cero. La confirmación de hermanas continúa hasta demostrar aislamiento real. El worker corre cada minuto y no reemplaza scans. | UM1.1, UM1.6 |
| PM-112 | Woo y ML tienen scans completos separados cada 15 minutos. Woo degrada salud a los 30 minutos y ML a los 60. Un cambio detectado por scan sin webhook converge y genera alerta/métrica de cobertura perdida. | UM1.2 |
| PM-113 | Una publicación ML pausada o sin stock con identidad inválida conserva caso no urgente; cerrada/eliminada archiva identidad e historia; reactivada con stock e identidad inválida se protege en cero y pasa a urgencia máxima. | UM1.2 |
| PM-114 | Familias obligatorias no cortan identidades verificadas existentes: generan deuda de catálogo con SLA de siete días y bloquean vínculos nuevos incompletos. Cambiar familia o atributos discriminantes revalida todos sus vínculos y conserva los exactos no contradictorios. | UM1.3 |
| PM-115 | Una línea con identidad insegura retiene el pedido completo. Se revisan pedidos abiertos/no conciliados desde la última evidencia confiable y se liberan cronológica e idempotentemente, respetando prioridad ML y deteniéndose si el stock no alcanza. | UM1.1, E1, E11 |
| PM-116 | “Observada” exige una jornada comercial completa. Rollback se prueba restaurando copia sanitaria y cambiando producción a `shadow/read-only`, sin reactivar legacy ni deshacer efectos remotos confirmados. El auditor recomienda; sólo el usuario declara `aceptada` una subentrega UM1. | UM1, gobierno |
| PM-117 | **Existe una capa de componentes compartida en `public/lib/components.css`**, construida sobre los tokens de `theme.css`. Es la idea de shadcn/ui sin React ni build: clases `ui-*` propias que cada pantalla importa en vez de redefinir. Motivo medido sobre las 27 pantallas: `.btn` estaba redefinido en 13, `.card` en 11, `.modal` en 6, `.tag` en 4 — arreglar foco, contraste o blanco de toque costaba trece ediciones. Reglas de la capa: sólo tokens (ningún color literal), `--accent` es **exclusivamente interactivo** (si algo es cian, se toca), blanco de toque mínimo 44px, y una pantalla extiende una clase pero nunca la redefine. Estrenada en Identidad de productos; las 27 migran progresivamente, no de golpe. | Transversal |
| PM-118 | **La telemetría de conciliación va en una línea de resumen, no en cinco tarjetas.** En 390px las tarjetas ocupaban la pantalla entera antes de mostrar nada accionable: el detalle del caso arrancaba fuera de la vista. Con la línea `.ui-resumen` arranca a 330px del borde superior. Los números son contexto; la tarea es el caso. Sólo lo que exige acción conserva peso visual (urgentes en ámbar, universo sin conciliar en rojo). El smoke E2E verifica la línea de conciliación, no las etiquetas de las tarjetas viejas. | UM1.1, UM1.5 |
| PM-119 | **Los atributos de ML se muestran con divulgación progresiva:** primero los que discriminan la variante (color, talle, medida, modelo, material, largo, peso), el resto detrás de un `<details>`. Una publicación típica trae 26 atributos planos y el operario tenía que leerlos todos para encontrar el que decide el vínculo. Medido en el caso de prueba: 6 discriminantes visibles + «Ver los otros 19». Las etiquetas de campo pasaron de `--accent` a `--txt`/`.ui-label` por PM-104: una etiqueta no se toca. | UM1.1, UM1.5 |
| PM-120 | **El `seller_sku` que trae la venta de ML cuenta como cobertura sólo si resuelve a EXACTAMENTE un producto del catálogo WC.** Decisión del usuario. En `_procesarOrden` convivían dos bloques que se contradecían: la guarda de Guardia ML retenía la orden entera antes de que el fallback anti-sobreventa pudiera usar ese `seller_sku`, dejándolo inalcanzable. Anular el fallback no era gratis —se escribió con datos medidos: 83 ventas sin mapear en 60 días, 54 con un SKU que sí existía— pero aceptar cualquier `seller_sku` podía descontar el producto equivocado. Con cero coincidencias no se sabe qué descontar; con dos o más, `buscarEnCache` desempata por menor stock, que sirve para elegir entre hermanas ya vinculadas pero no para decidir una identidad que nadie verificó: en ambos casos se retiene. Una clave que Guardia marcó divergente se retiene igual. Efecto de fondo: una orden con un ítem que no resuelve ya **no se sella** como `parcial` —sellar era lo que hacía que nadie volviera a mirarla, porque el cron no reprocesa una orden sellada— y queda retenida y visible. | UM1.1, E11 |
| PM-121 | **Cobertura legacy queda retirada**, confirmado por el usuario, cerrando la contradicción que dejó abierta el commit base `6949f02`. Su UI ya es inalcanzable (`/cobertura` y `/vinculos` redirigen a `/matcher/`) y sus mutaciones responden 410; ninguna llamada viva las alcanza. Los 50 casos que asertaban esas mutaciones quedan en pausa con el motivo escrito —no se borran, por si alguna vez se revive la interfaz unificada— y `test/cobertura-legacy-retirada.test.js` fija el contrato vigente: 410 incluso siendo admin (no es negativa de permiso, el endpoint ya no existe), la lectura sigue viva y `POST /actualizar-ml` sobrevive como alias compatible. | UM1.1, UM1.6 |
| PM-122 | **Un test puede estar verde por una base rancia.** `matcher-ml-robusto-hito4` escribe su propio `CREATE TABLE` a mano en vez de usar el bootstrap real y había quedado seis columnas atrás de lo que inserta `prepararUpsertCache` (las de la migración 082); venía pasando sólo porque reusaba un `.sqlite` viejo de `test/` que sí las tenía, y falló apenas se limpiaron los temporales huérfanos. Conviene `git clean -f -x test/` antes de una corrida que se vaya a creer. Un `-journal` rancio, además, hace fallar la corrida siguiente con `SqliteError: disk I/O error`, que no es el bug real. | Transversal |
| PM-123 | **Una publicación fuera del universo activo con una decisión que ML no refleja es deuda dormida, no «fuera de alcance».** Se cerraba como `resuelto` y quedaba invisible. El cron legacy que la corregía (`pushSkusPendientes`) **ya no está agendado** —`matcherPush` ni se importa en `server.js`—, así que hoy nada la mira. Observadas 3 en producción, cada una con el SKU de OTRO producto: `MLA1401411650|180043410439` (decidido FB-12648, ML lleva FB-6325), `MLA2000138388|192504429779` (FB-50847 vs FB-50523) y `MLA1927478426|187049488547` (FB-22679 vs FB-12156). El corte es **estrecho a propósito**: exige una decisión divergente. Marcar toda pausada sin SKU daba **4047 casos** medidos contra la base real y enterraba la cola; y una pausada sin SKU no es peligrosa, porque al reactivarse sigue sin SKU y el pase activo la toma. Se registran con severidad `normal`: visibles sin contar como trabajo humano, porque la conciliación cuenta sólo el universo activo con stock. | UM1.1, UM1.2 |
| PM-124 | **Una decisión tomada que ML no refleja invalida la identidad, aunque el SKU que ML lleva exista y sea único en Woo.** Sin esto la publicación se auto-verificaba contra el producto **equivocado**: `clasificarClaveMl` sólo mira lo que ML tiene, no lo que se decidió, así que un SKU ajeno pero válido clasificaba `sku_exacto` y quedaba verificado. Es el daño concreto que abrían las pausadas huérfanas al reactivarse. La clasificación pasa a `decision_no_aplicada` y el caso apunta al producto **decidido**, no al que ML lleva por error. Medido contra la base real: no da vuelta ninguna publicación activa (0 decisiones divergentes activas), la conciliación queda igual (1088 total, 1086 verificadas, 2 urgentes, conciliado) y aparecen exactamente las 3. | UM1.1 |
| PM-125 | **Hay dos escritores de `SELLER_SKU` en ML, no uno.** El comentario de cabecera de `lib/identidadMl.js` afirmaba ser «el ÚNICO lugar del programa que escribe identidad o stock»; es falso: el worker de Guardia (`procesarOperacionesGuardia`, cada 5 minutos en `server.js`) escribe el mismo campo vía `escribirSkuEnMl` y además puede desvincular y pausar. La colisión no es teórica: **26 operaciones de Guardia en estado `conflicto`**, todas frenadas por su control de versión. No se puede desagendar de una: el botón «vincular» del Matcher —enlazado desde el home— alimenta esas operaciones por `POST /api/guardia-ml/vincular-clave`. Orden correcto: migrar ese endpoint a UM1 y recién después desagendar el worker. Queda como entrega de UM1.6. | UM1.6 |
| PM-126 | El chip del home decía **«SKUs por escribir en ML»** y apuntaba a `sync-detalle`, prometiendo una acción que ningún proceso ejecuta desde que `pushSkusPendientes` dejó de agendarse. Pasa a decir «SKUs decididos que ML no refleja» y apunta a Identidad de productos, que es donde ese trabajo vive ahora. | UM1.1, UM1.5 |
| PM-127 | **Cargar y corregir un tracking son acciones de despacho, no de preparación de la mercadería** (decisión del usuario, 2026-09-05). Cargarlo: Administración puede resolverlo aunque la preparación esté tomada por otro; un operario sin la toma sigue necesitándola, para no usurpar la tarea de un par. Corregirlo: **se retira el requisito de toma** y lo resuelve quien tenga escritura en Preparación o Administración, esté tomada por quien esté — el control pasa a ser la auditoría, no el turno, y el evento `tracking_corregido` ya registra quién fue. Sin permiso de Preparación responde 403. Usa el mismo patrón de permisos que el resto de los routers (`permisos.some(p => p.herramienta === 'preparacion' && p.nivel === 'write')`). | E2, E4 |
| PM-128 | **Que la suma de stock expuesto en ML supere el stock real de Woo es INTENCIONAL, no un defecto** (decisión del usuario, 2026-09-05). Cuando un SKU está en varias publicaciones, cada una lleva el stock completo a propósito: es la forma de ofrecer la misma unidad por varias vías, y el resync de hermanas corrige apenas se vende. Medido: **213 SKUs exponen 1108 unidades de más**, y 212 de ellos son multi-publicación (p. ej. FB-29522: 2 publicaciones × 35 = 70 expuestas contra 35 reales; FB-2864: 1 unidad real en 2 publicaciones). **UM1 no debe marcarlo como riesgo de sobreventa ni intentar repartir el stock.** Queda asentado explícitamente porque a simple vista parece un error grave y es el tipo de cosa que una sesión futura «arregla» sin preguntar. La ventana de carrera entre la venta y el resync se acepta. No confundir con el caso distinto de una publicación cuyo stock ML no coincide con Woo siendo la ÚNICA publicación del SKU: eso sí es `stock_no_verificado` y sí es trabajo. | UM1.1, E11 |
| PM-129 | **Un solo escritor por clave ML.** `POST /api/guardia-ml/vincular-clave` —el botón «vincular» del Matcher, enlazado desde el home— encolaba siempre una operación de Guardia, así que dos schedulers podían tocar la misma publicación: el worker de identidad cada minuto y el de Guardia cada cinco. Ya había producido **26 operaciones de Guardia en `conflicto`**, frenadas sólo por su control de versión. Ahora, si Identidad gobierna la clave (caso no `resuelto`), el endpoint **delega** en `decidirCasoIdentidad` en vez de rechazar: el usuario hace lo mismo de siempre y la escritura sale por un único camino, con caso, decisión, operación durable, verificación por relectura e intervención ante fallo. Si Identidad no la gobierna, sigue por Guardia. El freno por hermanas devuelve 409 apuntando a la pantalla de Identidad: confirmarlo a ciegas desde el Matcher vaciaría el freno. Cinturón y tirantes en el worker de Guardia: una operación encolada antes de la delegación sobre una clave gobernada termina en `conflicto`, no en reintento —no es un fallo transitorio. **Impacto medido hoy: cero operaciones frenadas.** Los 67 casos vivos de Guardia son links de pago de Mercado Pago, que UM1 no gobierna. La guarda es preventiva. Desagendar el worker de Guardia queda para cuando esos 67 tengan destino. | UM1.6 |
| PM-130 | **Corrige a PM-123/PM-124.** Ante una decisión local que contradice el `SELLER_SKU` de ML, el caso apunta a lo que dice **ML**, no a lo que dice la decisión. La primera versión hacía lo contrario —daba por buena la decisión histórica— y proponía matches absurdos: en `MLA1927478426|187049488547` ML dice `FB-12156` (Zion Diablo Carbono 1x12, con el GTIN `7798366205223` corroborándolo en tres publicaciones) y la decisión vieja decía `FB-22679`, que es una Zion **Strix** 1X11v, otra bicicleta. Lo detectó el usuario mirando la pantalla. `SELLER_SKU` es identidad y una decisión histórica no la reemplaza; la contradicción se conserva en la evidencia (`decision_divergente`) y la resuelve una persona. **También cambia el riesgo declarado**: no es «si se reactiva vende mal» —ML tiene la identidad correcta— sino «hay una decisión vieja equivocada esperando que algo la aplique», que es justo lo que habría hecho el cron de push muerto. | UM1.1 |
| PM-131 | **Un caso sin fila en `identidad_evidencias` hace que la pantalla muestre huecos que se leen como datos.** El pase de deuda dormida creaba el caso pero no la evidencia —el del universo activo sí—, y la pantalla mostraba «Publicación: —» y «SELLER_SKU: ausente» cuando ML decía `FB-12156`. Un hueco que se ve como un dato es peor que un error visible: el operario decide sobre información falsa. Todo pase que cree un caso escribe su evidencia en el mismo acto. | UM1.1, UM1.5 |
| PM-132 | **Un `user_product` de ML es UNA bolsa de stock. Si dos publicaciones lo comparten y están mapeadas a Productos Fusion distintos, el stock se pisa para siempre y una de las dos vende sin existencia.** Es un problema de IDENTIDAD, no de stock. Verificado por lectura autenticada (2026-09-05): tres pares comparten `user_product` y son productos genuinamente distintos —cadena normal vs **OEM** (FB-1805/FB-32234), maza **28H vs 32H** (FB-28334/FB-3789), horquilla **Tapered vs Boost** (FB-4501/FB-4746)—. Woo los tiene separados con stock propio; ML los agrupó. Ejemplo del daño: la bolsa tiene 4, FB-1805 pide 0 y FB-32234 pide 4; gana el 4 y **FB-1805 queda con 4 unidades a la venta con existencia 0**. El síntoma visible era un bucle de reconciliación cada ~3 h que logueaba `ok` sin efecto. **Invariante nuevo para UM1: un `user_product` ⇒ un Producto Fusion.** Su violación garantiza sobreventa y hoy no se detecta. Requisito previo barato: poblar `user_product_id` en el refresco —viene en el payload del ítem y la columna existe desde la migración 082, pero está NULL en las 6894 filas—; con eso la detección es local y gratis. **Dos hipótesis previas descartadas por evidencia**: no es que ML ignore el PUT por tener `user_product_id` (el 88% de una muestra de 60 lo tiene y funciona), ni que sea por `catalog_listing` (sólo uno de los tres lo es). | UM1.1, UM1.3, E11 |
| PM-133 | **`user_product_id` se toma del campo de primer nivel del ítem, no de los atributos.** `evidenciaIdentificadores` lo buscaba con `attrValor(combinados, ['USER_PRODUCT_ID'])` y devolvía `null` siempre: las **6894 filas** del cache quedaron en NULL desde la migración 082, y sin ese dato el sistema no podía ver que dos publicaciones comparten una misma bolsa de stock. La petición a ML ya lo pedía y el upsert ya tenía la columna; el único hueco era la extracción. En variaciones se usa el de la variación y, si no tiene, se hereda el del ítem. | UM1.1, E11 |
| PM-134 | **`conflictosDeBolsaCompartida` agrupa por el producto del CASO, no por la identidad activa.** Una identidad sólo se activa cuando el caso queda verificado, y estos conflictos impiden precisamente que se verifique —las dos publicaciones se pisan la cantidad, así que el stock nunca coincide—. La primera versión se apoyaba en `identidades_canal` y **no habría detectado ni uno solo de los casos reales**. | UM1.1 |
| PM-135 | **Medición contra producción (2026-09-05, solo lecturas):** 927 publicaciones activas leídas, **874 (94%) tienen `user_product_id`**, y hay **3 conflictos de bolsa compartida** entre las activas: `MLAU3086754975` (FB-3789/FB-28334, maza 28H vs 32H), `MLAU3210195462` (FB-1805/FB-32234, cadena vs OEM) y `MLAU402482129` (FB-21141/FB-21145, maza **Boost 15×110 vs estándar 15×100**). El tercero **no daba síntoma**: sus cantidades coinciden hoy por casualidad, así que no producía el bucle de reconciliación — pero la bolsa tiene 1 unidad y las dos publicaciones muestran 1, o sea sobreventa si se venden ambas. Es el valor concreto del detector: encuentra el conflicto latente, no sólo el que ya está fallando. Hay al menos uno más entre las pausadas (FB-4501/FB-4746, horquilla Tapered vs Boost), fuera del universo activo. | UM1.1, E11 |
| PM-136 | **El topic `items` de ML deja de ser audit-only y pasa a proyectar** (decisión del usuario, 2026-09-05, cerrando la pregunta que §15 del plan dejaba abierta). Llegaban ~1000 avisos diarios que se registraban y se tiraban mientras la detección de un cambio dependía del scan de 15 minutos. **Condición explícita del usuario: no aumentar las llamadas a la API de ML** —la cuenta ya fue bloqueada una vez. Se cumple con tres cosas: relectura acotada por multiget (`refrescarPublicacionesMlAcotado`, nunca el scan completo), coalescencia de ráfagas de 2 minutos por ítem (ML manda **2,6 avisos por ítem**: ~1000 eventos para ~380 ítems únicos) y el limitador de cupo existente. **Costo medido: ~19-380 llamadas/día contra las ~34.000/día que ya gasta el scan (172 ciclos × 197 llamadas): entre +0,06% y +1,1%.** | UM1.2, E11 |
| PM-137 | **La proyección de mensajes de ML no se puede arreglar desde el código.** El webhook `messages` v1 manda el `resource` como id pelado, y el único endpoint que lo resuelve es `/marketplace/messages/{id}`, que devuelve **403 `Invalid caller.id`**: pertenece al flujo de aplicaciones *marketplace* (global selling), no al de un vendedor común. Se descartó que fuera el bloqueo «Model 6» que documenta ML —ése daría 403 también en `/messages/packs/...`, y ahí obtenemos 404—, y el usuario confirmó que la app tiene mensajería habilitada. Quedan 39 jobs `dead_lettered` y `ml_mensajes` en 0 filas: **nunca funcionó**. Corregir sólo la URL convertiría 39 errores de DNS en 39 de permiso. Pendiente de decidir entre descartar el topic explícitamente o alcanzar los mensajes por `/messages/packs/{pack}/sellers/{id}` desde el contexto de una orden. | UM1.2 |
| PM-138 | **La cadencia del scan ML y su ventana de frescura son adaptativas, se mueven JUNTAS y se ganan con evidencia, no con calendario** (decisión del usuario, 2026-09-05). Escalones: 15/60 → 20/60 → 30/60 → **60/120**. **Por qué juntas**: un scan de 60 minutos con frescura de 60 es la combinación rota — justo antes de cada corrida toda observación tendría ~60 minutos y **nada verificaría**, vaciando el tablero. **Por qué evidencia y no almanaque**: el disparador es la métrica que el plan ya pedía —un cambio detectado por el scan que ningún webhook `items` anunció—. Uno solo basta para bajar un escalón. Subir cuesta 288 corridas limpias consecutivas (~3 días) en los primeros escalones y **2900 (~2 meses) en el último**, porque ése no cambia un parámetro sino el invariante «observación confiable» del programa. Si la proyección de `items` falla, el ramp **se congela y además baja** —quedarse en un intervalo largo sin webhook es quedarse ciego—. El último escalón se sostiene sólo porque, con el webhook cubriendo, una observación vieja significa «nada cambió» y no «no miramos». | UM1.1, UM1.2, E11 |
| PM-139 | **El webhook de `items` es una segunda vía de refresco del precio de ML, y se evaluó contra el acoplamiento que `routes/sync.js` advierte por escrito** («si el día de mañana se agrega OTRA vía de cambio de precio de ML, esa vía tiene que sumarle el mismo par actualizar-caché/borrar-frenada… no hay ningún chequeo automático que detecte esta desviación»). Planteado por el usuario antes de desplegar: un cambio de precio podría reactivar publicaciones. **Veredicto: es seguro y además mejora la red existente.** `necesitaRecheck` re-consulta justo cuando el precio del caché difiere del que produjo la frenada, así que un precio más fresco hace que la diferencia se DETECTE en vez de saltearse. No hace falta borrar la frenada: la fila se sobrescribe al re-evaluarse. Y ninguna reactivación sale sin revalidación en vivo contra ML (`chequearNetoReactivar`, fail-closed): cambia el **cuándo**, no el **si**. Exposición medida: 7 frenadas y **12 filas realmente reactivables** de 4621 pausadas, o sea que el costo en llamadas está acotado por esas 12 y no por el volumen de webhooks. El comentario de `routes/sync.js` quedó actualizado para que la próxima vía no repita el análisis desde cero. | UM1.2, E11 |
| PM-140 | **Se captura QUÉ cambió en una publicación de ML, campo por campo** (`ml_cambios_observados`, migración 087). Antes se registraba que llegó un aviso (`integration_events`) pero no su contenido, así que preguntas operativas básicas no tenían respuesta: con qué frecuencia ML cambia un precio por su cuenta, cuántas veces pausa una publicación, si alguien edita el `SELLER_SKU` a mano por fuera de la herramienta, si el stock se mueve sin que haya venta. Esa última dejó de ser teórica: dos publicaciones que comparten `user_product` se pisan la cantidad y el único síntoma era un bucle que nadie miraba (PM-132). Campos observados: `seller_sku`, `available_quantity`, `status`, `sub_status`, `precio`, `user_product_id`, `gtin`. **Es captura, no acción**: nadie decide nada con esta tabla todavía, y la escritura es fail-open para no tumbar la proyección, que es lo que de verdad importa. | UM1.2, E11, E22 |
| PM-143 | **Corrige a PM-141.** La reconciliación descarta las preguntas con `deleted_from_listing: true` (eliminadas de la publicación) o `hold: true` (retenidas por ML): no son trabajo pendiente y **no se pueden responder**, así que traerlas sería ruido inaccionable en una bandeja. Verificado contra la documentación oficial de ML y contra la API. Esto corrige un error de mi diagnóstico anterior: había señalado `MLA1820067925` (35 días, publicación activa con stock) como «el aviso perdido de verdad» y resultó tener `deleted_from_listing: true`. De las 6 que faltaban, **2 son descartables y 4 son trabajo real**. Dato de contexto que aportó el usuario: **el panel de ML sólo muestra 30 días hacia atrás**, así que 5 de esas 6 son invisibles para él aunque la API las devuelva —la herramienta puede mostrar lo que ML ya no muestra, que es parte del valor de la red—. | UM1.2, E6 |
| PM-141 | **Las preguntas de ML tienen red de reconciliación: `reconciliarPreguntasMl`, cada 20 minutos.** Se poblaban sólo por webhook y ML no garantiza entrega. Medido contra la API el 2026-09-06: de **10 preguntas sin responder, 6 no estaban en la base** —el usuario lo confirmó desde su pantalla: veía 4—. De esas 6, **5 eran de publicaciones pausadas sin stock** (la explicación que aportó el usuario, correcta) y **1 era de una publicación activa con stock**, del 1 de agosto: ése es un aviso perdido de verdad y la prueba de que la red hace falta más allá del stock. Se pide sólo `status=UNANSWERED` —verificado en vivo, `GET /questions/search?seller_id=&api_version=4&status=UNANSWERED`—: es el subconjunto que exige acción humana y mantiene el costo en UNA llamada por corrida, contra las 1111 preguntas del histórico. Idempotente, con la misma guarda de no pisar una respondida. | UM1.2, E6 |
| PM-142 | **Las preguntas de publicaciones pausadas o sin stock se traen igual, pero se separan** (decisión del usuario, 2026-09-06). Una consulta sin responder lo es igual, y además dice qué quiere gente de algo que no tenemos —señal de reposición—; pero no se resuelve vendiendo y no debe competir con las que sí. El contador expone `preguntas_sin_stock`. Si el cache de publicaciones no está disponible devuelve `null` = «no sé», nunca 0: cero afirmaría que ninguna está sin stock. Una publicación que ni siquiera está en el cache tampoco cuenta como sin stock, por lo mismo. Nota de alcance: hoy responder se hace en ML y la herramienta sólo cuenta (decisión del 2026-08-26), pero el usuario avisó que **responder desde acá se va a implementar**, y ahí esta separación pasa a valer bastante más que en un contador. | UM1.2, E6 |
| PM-144 | **La protección Woo→ML se parte en dos: detección ahora, escritura después** (decisión del usuario, 2026-09-06). Medido antes de diseñar: la exposición hoy es **cero** —ninguna publicación activa vende contra un SKU inexistente, ninguna identidad Fusion apunta a un producto Woo ausente—, así que sería preventiva y no correctiva. Y **la retención de pedidos YA funciona sin tocar nada**: las dos vías de cobertura de `_procesarOrden` exigen que el SKU exista en `catalogo_cache` (`buscarEnCache` y `skuUnicoEnCatalogo`), así que una venta de una clave huérfana se retiene sola. Lo que faltaba era enterarse **antes** de que llegue esa venta: hasta ahora la única señal era un pedido retenido, o sea con el cliente ya habiendo comprado. `publicacionesSinRespaldoWoo` lo detecta y la pantalla lo avisa. Poner el stock remoto en cero y bloquear la publicación —el resto de PM-104— queda como entrega aparte con plan escrito y canario, porque escribe en ML. | UM1.2, UM1.3 |
| PM-145 | Los dos pendientes de Ventas por **SKU inexistente en Woo** (`FB-67156` y `FB-68312`) **se cerraron solos**: al 2026-09-06 ambos existen en el catálogo con stock coincidente y sus casos quedaron `verificado`. No requirieron acción de UM1. | UM1.1 |
| PM-146 | **El topic `messages` queda auditado, sin proyección** (cierra la decisión que §15 dejaba abierta). No es preferencia: **no se puede proyectar**. Verificado contra la API y la documentación oficial — el webhook manda el `resource` como id pelado y el único endpoint que lo resuelve, `/marketplace/messages/{id}`, responde 403 `Invalid caller.id` porque pertenece al flujo de aplicaciones *marketplace*, no al de un vendedor común; el usuario confirmó que la app ya tiene mensajería habilitada, y se descartó el bloqueo «Model 6» (ése daría 403 también en `/messages/packs/…`, donde obtenemos 404). Mantenerla costaba **39 jobs muertos y 352 reintentos en 7 días** y dejaba la cola de dead-letters permanentemente roja, tapando los fallos que sí importan. El evento se sigue recibiendo, deduplicando y registrando; `ingerirMensaje` queda intacta a propósito para volver a enrutar si algún día hay acceso. | UM1.2, E6 |
| PM-147 | **Los jobs muertos ahora se ven** (brecha 6 del documento de arquitectura). `GET /api/notificaciones-ml/dead-letters` los agrupa por tipo y motivo —lo que importa operativamente es qué clase de cosa falla y desde cuándo, no cada fila— y el contador liviano los expone para el aviso del home. Hasta ahora **nada los mostraba ni los reclamaba**: los 42 muertos que encontramos sólo aparecieron porque alguien fue a mirar la base a mano. | UM1.2, E6 |
| PM-148 | **Probar el cable, no sólo las puntas.** El ramp del scan no gobernaba nada y los tests estaban verdes: `identidadProductos` usaba `frescuraVigenteMs` sin haberla importado y el `try/catch` de respaldo se tragaba el `ReferenceError`. Había cobertura en los dos extremos —la función en `mlScanRamp.test.js`, el consumidor en `identidad-productos.test.js`— y **cero sobre la conexión**. Queda un invariante que afirma el efecto: cambiar la frescura del ramp tiene que cambiar el veredicto de salud. Regla general: cuando un valor viaja entre módulos, se prueba el viaje. Se descartó por medición prohibir los `catch` mudos —hay **24 en 17 archivos** y muchos son legítimos—; y queda anotado que **el proyecto no tiene linter**: un `no-undef` habría marcado esto en el acto y es la única defensa que no depende de que alguien se acuerde. | Transversal |
| PM-149 | **El proyecto tiene ESLint, con gate sobre los archivos que toca el diff** (decisión del usuario, 2026-09-06: config completa, gate desde el primer día pero acotado al diff). Existe por un bug concreto: `lib/identidadProductos.js` usaba `frescuraVigenteMs` sin importarla, un `catch` se tragaba el `ReferenceError` y la cadencia adaptativa del scan **no gobernaba nada con la suite en verde**. `no-undef` lo marca en el acto y es la única defensa que no depende de que alguien se acuerde de escribir un test. **Deuda medida al adoptarlo: 240 archivos, 151 errores y 81 avisos**, dominados por 138 `no-empty` (bloques `catch {}` heredados). Por eso el gate es `npm run lint:diff` y no `npm run lint`: sirve desde el día uno sin exigir limpiar antes lo viejo. Los 25 `no-undef` y 2 errores de parseo de la primera pasada eran **falsos positivos de la configuración** —`window`/`document` dentro de `page.evaluate()`, un Web Worker, y dos archivos ESM marcados como script— y se corrigieron en la config, no silenciando la regla. En su primera corrida sobre un diff real encontró **código muerto**: un contador `incompletas` que se incrementaba y nunca se leía. Nota de despliegue (corregida el 2026-09-06 al desplegarlo): agrega `eslint`, `@eslint/js` y `globals`, y **las tres son devDependencies**, así que producción **no** necesita `npm install` —el servidor no las carga y saltearlo evita que npm toque el `node_modules` productivo, que es justo lo que rompió un worktree ese mismo día. El lint corre donde se desarrolla. | Transversal |
| PM-150 | **El GTIN se guarda en la forma canónica GS1 de 14 dígitos, y el tipo se deriva de la representación recibida.** `normalizarGtin` no normalizaba: validaba el largo y devolvía el código crudo, así que `0602883701731` y `602883701731` eran dos identificadores distintos. Medido el 2026-09-06 sobre producción: **13 de los 650 pares SKU comparables entre ML y Woo eran el mismo código con distinto relleno** y se leían como diferentes; al canonizar, **17 publicaciones ML encuentran respaldo en Woo que antes no encontraban y ninguna pierde un match**. La regla es de GS1 General Specifications: almacenar en 14 dígitos, justificado a la derecha con ceros, y «adding fill zeros does not change a GTIN-8, 12 or 13 into a GTIN-14». El canónico se arma **rellenando, nunca quitando**: en un UPC-A como `036000291452` el cero inicial es el sistema numérico y sacarlo destruye el código —lo detectó un test antes de que tocara ningún dato. El `subtipo` describe **cómo entregó el código cada canal**, sin inferir un tipo «verdadero» bajo el relleno: una vez rellenado, un UPC-A y un EAN-13 son indistinguibles y adivinar sería inventar un dato que ningún canal da. Ni ML ni Woo exponen el tipo: ML publica un único atributo `GTIN` (5385 de 6893 publicaciones, sin un solo `EAN`/`UPC`/`GTIN_14` aparte) y Woo un único `global_unique_id`. El `UNIQUE` global de tabla pasa a **índice parcial sobre `estado='activo'`** y se suma el estado `conflicto`: sin eso un código en conflicto no se podía ni registrar —la segunda fila fallaba y el `INSERT OR IGNORE` la descartaba en silencio—, lo contrario de lo que pide la máquina de estados. Migraciones 088 y 089, verificadas sobre copia de producción. | UM1 / Backend |
| PM-151 | **Cuál identificador representa a un producto se decide por una lista de prioridad reordenable, no por una regla fija** (decisión del usuario, 2026-09-06: «gana la elección en productos Fusion, configuración de lista por prioridad cuál gana, si tiene 4 distintos dejar ordenar por cuál empezar, así como el sistema de booteo de una PC»). Se descartó la alternativa que yo recomendaba —«Woo siempre gana como principal»— por ser una regla fija que no sobrevive al caso de varios códigos legítimos. Un producto puede llevar varios GTIN válidos: son los que el mismo artículo tiene en distintos mercados (**210 pares SKU con GTIN distinto entre ML y Woo**, por ejemplo Shimano-JP `4524667220343` contra Shimano-US `689228220348`). Tras sembrar los GTIN de ML quedan **39 productos con más de un identificador activo**, y el orden los resuelve a los 39 sin intervención. Esto **supera el invariante «una unidad admite como máximo un EAN activo y un UPC activo»**: es falso contra los datos —**22 productos tienen dos activos del mismo subtipo**, con códigos válidos— y limitar a uno por familia obligaría a descartar identificadores reales. `identidad_config.identificadores_prioridad` fija sólo el orden **inicial** (`woo,ml,manual` por defecto, para que la proyección no cambie en silencio lo que Woo ya publica); el orden por producto se reordena desde Productos Fusion. `orden` deliberadamente **no lleva índice único**: con `UNIQUE`, reordenar exigiría swaps en varios pasos y un `UPDATE` simple fallaría a mitad; el desempate es determinista igual por `(orden, id)`, sin lo cual la proyección a Woo podría cambiar sola entre dos corridas. | UM1 / Backend + Ventas |
| PM-152 | **El ramp del scan corre un tick más lento que su intervalo nominal, y se deja así.** `tocaScan` compara contra el intervalo exacto, y el scan se registra unos milisegundos después del tick que lo lanzó (medido el 2026-09-06: tick 15:45:00.0xx contra `ultimo_scan_en` 15:30:00.**160** → faltan 0,16 s, no dispara, corre 15:50). Como el nuevo timestamp vuelve a quedar tarde, el desfase se repite: **la cadencia real de cada escalón es 15→20 min, 20→25, 30→35, 60→65**. Predicho y confirmado en producción el mismo día: el scan se salteó las 15:45 y corrió 15:50:00.136. Consecuencia incómoda: el ramp está en su escalón más agresivo y nunca lo cumplió, así que las cuentas de frescura previas estaban 33% infladas. Se descartó corregirlo con una tolerancia (decisión del usuario): pasaría de 72 a 96 scans diarios, de **14.184 a 18.912 llamadas a ML por día**, contra la API que ya nos bloqueó una vez. Queda documentado en `tocaScan`, en la tabla `ESCALONES` y fijado por un test que dice explícitamente que romperlo cambia el consumo; si algún día se corrige, hay que bajar un escalón el ramp en la misma tanda. | UM1 / Backend |
| PM-153 | **Las bicicletas Venzo no tienen código universal posible: su GTIN se marca `incorrecto` sólo en el registro local** (decisión del usuario, 2026-09-06). Están publicadas con códigos que no les corresponden —el testigo es `4550170140663`, prefijo `455017` de Shimano, o sea un código de componente pegado a una bicicleta, compartido por 126 publicaciones Venzo—. Alcance acotado a lo que está pausado o sin stock: una publicación activa y vendiendo no pierde su identidad sin mirarla una por una. Se descartó vaciar el atributo en ML: son ~818 publicaciones, y mientras estén pausadas o sin stock nadie compra contra ese código, así que no justifica gastar cuota de la API que ya nos bloqueó. Las 818 publicaciones colapsan a **82 identificadores en 81 productos**, que es la unidad real. Efecto verificado sobre copia de producción: conflictos de 41 a 38. **Corrección a lo que yo había reportado**: el código de 117 productos que señalé como caso testigo (`00710845783784`) NO es Venzo —419 publicaciones, sólo 14 Venzo; el resto son componentes Shimano y SRAM—, así que es un comodín de fabricante y un problema distinto. De los 41 conflictos, 11 tocan Venzo y 31 no: el usuario los dejó explícitamente como «necesitan resolverse apenas se pueda pero no nos bloquea a seguir desarrollando todo el plan». `marcarIdentificadorIncorrecto` exige `permitirUnico` para dejar a un producto sin GTIN: que pierda identidad tiene que ser una decisión, no un efecto colateral. | UM1 / Ventas + Backend |
| PM-154 | **La protección Woo→ML de PM-104 se entrega en dos mitades, y la remota reusa el ejecutor de la saga en vez de tener el suyo.** Hasta el 2026-09-06 el webhook de producto Woo sólo actualizaba el catálogo local: borraba la fila y las publicaciones de ML seguían vivas vendiendo. La detección existía (`publicacionesSinRespaldoWoo`), la protección no, y `direccion='woo_ml'` estaba en el esquema desde la migración 082 **sin un solo caso ni una referencia en código**. Mitad 1 (local, sin tocar ML): confirma la baja, abre caso `woo_ml` crítico por clave y retiene los pedidos que todavía no bajaron a Woo. Mitad 2: pone stock cero en ML y lo verifica. Se descartó darle a la protección su propia tabla y su propio worker —la maquinaria de seguridad (modo + flag, canario, lote máximo, claim, reintentos, verificación remota por paso) es justamente lo que no conviene duplicar, porque es donde los frenos divergen con el tiempo— y también se descartó inventar una decisión y un SKU falsos para que entrara en el molde actual, que es acomodar el dato al esquema. La migración 091 agrega `tipo` y hace nullables `decision_id`, `producto_id` y `sku_objetivo`, con un `CHECK` que impide una `correccion_sku` sin decisión ni objetivo: el invariante vive en la base, no sólo en el código. **Dos fallas atrapadas antes de producción**: recrear la tabla rompía **459 referencias** de `identidad_operacion_pasos` —el RENAME reescribe las claves foráneas de la hija hacia `_old` y al soltarla quedan colgando; se resuelve con `legacy_alter_table` y lo detectó el `foreign_key_check` agregado como guarda— y la 091 corría **antes** que la 085 porque el orden en `db/index.js` no es numérico, así que `sin_cero` todavía no existía. `clavesAfectadasPorBajaWoo` busca por identidad activa **y** por SKU: una publicación puede llevar el SKU sin haberse verificado nunca, y es justo la que nadie miró. `confirmado` es obligatorio —un `product.deleted` firmado por Woo ya cuenta— porque sin esa distinción un webhook espurio retendría pedidos de productos vivos. Freno de activación (decisión del usuario): se reusa `canario_ml_key`, que hay que **poner antes de desplegar** junto con `lote_max=1`; hoy el canario está vacío y el lote en 25. | UM1.2 / Backend |
| PM-155 | **Un webhook de Woo `disabled` es una pérdida silenciosa de eventos, y hasta el 2026-09-06 nada lo miraba.** Woo desactiva un webhook por su cuenta tras varias entregas fallidas: deja de mandar eventos y todo parece normal. Al implementar la vigilancia se encontró que **`order.updated` ya estaba `disabled` en producción** (id 7), sin fecha conocida de caída. `lib/wooWebhooks.js` relee el estado cada hora —una sola llamada, no mueve el consumo— y guarda `status_desde`, que sólo se mueve cuando el estado cambia de verdad: reescribirlo en cada corrida haría que un webhook caído hace días pareciera recién caído, y ése es justo el dato que dice cuántos eventos pueden haberse perdido. `paused` cuenta igual que `disabled`. Sólo se vigilan los propios: la tienda tiene webhooks de terceros (`prometheo-be.itesa.co`) que no administramos. **Reactivar no se hace solo**: es una escritura sobre la configuración de la tienda y una decisión de operación —si se cayó por entregas fallidas, reactivarlo sin arreglar la causa lo vuelve a caer—. | UM1.2 / Operaciones |
| PM-156 | **Cierre de las brechas Media y Baja de UM1** (2026-09-06). *Archivado*: `archivarIdentidadesMlHuerfanas` cierra las identidades ML cuya publicación ya no existe —contaban como cobertura y nunca se podían verificar, porque no hay qué leer—. Corre **sólo tras un scan completo**, y ésa es toda la seguridad: el criterio es «no está en el cache», así que con un refresco acotado o interrumpido archivaría identidades vivas que simplemente no se leyeron; `lecturaConfiable` no tiene default a propósito. No borra: `activa=0` + `archivado_en`, y la reactivación existente la recupera. *Métricas*: `saludPipelineEventos` expone eventos que se ingirieron y **nunca derivaron en trabajo** —el agujero que no tenía nadie mirando, porque no falla, no reintenta y no aparece en dead letters—, dead letters por tipo, y jobs que agotaron intentos sin quedar marcados como muertos. Medido al cerrarla: 0 eventos sin job, 42 dead letters (39 `message.project` de PM-137, 3 `question.project`). *Legacy*: la pantalla de Operaciones anunciaba «stock cero → limpiar SKU → escribir SKU → restaurar stock» como si fuera el único camino, cuando una corrección hacia un SKU válido se escribe de una sola vez y el cero sólo cubre la ventana sin SKU; ahora describe los dos caminos y muestra el tipo de cada operación. Las menciones a «cada 5 minutos» que quedan describen crons vigentes y no son legacy. | UM1 |
| PM-157 | **Los mensajes post-venta se leen por pack, no por el id del aviso, y no hacía falta ningún permiso nuevo** (verificado contra la API el 2026-09-06). Corrige el diagnóstico de PM-137. Medido: `/marketplace/messages/{id}` → **403 `Invalid caller.id`** (es el recurso de las apps de mensajería, no el de un vendedor), `/messages/{id}` → **404**, `/messages/unread?role=seller&tag=post_sale` → **200**, `/messages/packs/{pack}/sellers/{seller}` → **200**. Los 39 jobs muertos ni siquiera llegaban al 403: concatenaban el id pelado a la base y pedían `api.mercadolibre.com01a0725…`, o sea que **nunca salieron a la red**. Como el id del aviso no es resoluble por ninguna vía de vendedor, el webhook pasa a disparar `reconciliarMensajesMl`, que además repara lo perdido porque no depende de que el aviso llegue —se suma un cron cada 20 minutos, igual que preguntas—. **`mark_as_read=false` es obligatorio**: sin ese parámetro, LEER marca los mensajes como leídos en ML, y una reconciliación automática no puede decidir por una persona que ya vio un mensaje; hay un test que lo fija. Verificado contra producción: 10 mensajes reales proyectados —incluido `01a07256e97…`, uno de los 39 dead letters— con su pack y su pedido, y el contador de no leídos de ML **quedó intacto tras tres lecturas**. La doc de ML sólo excluye a los vendedores «Modelo 6», que no es el caso. | UM1.2 / Backend |
| PM-158 | **El historial de identidad guarda cambios, no cada corrida; y las repeticiones ya acumuladas se purgan conservando la primera y la última de cada caso** (decisión del usuario, 2026-09-06). `identidad_historial` tenía **177.500 filas y 176.444 —el 99,5%— eran el mismo evento `identidad_verificada` repetido**: se anotaba uno por clave verificada en CADA auditoría, ~1.130 por corrida y unas 81.000 por día. Era además la tabla más pesada de la base (20,6 MB). Eso no es historial sino telemetría, y sepultaba los 842 eventos que sí importan —decisiones, conflictos, archivados—. **El emisor**: ahora registra sólo cuando cambia la identidad (nueva, reactivada, o con distinta huella de SKU/GTIN/clave); un cambio de stock no anota, porque la huella es de identidad y una cantidad distinta no convierte a la publicación en otro producto —lo aclaró un test escrito primero al revés—. La identidad se sigue refrescando en cada corrida con su `observado_en` y su stock, o la frescura mentiría. Verificado sobre copia: dos auditorías seguidas suman 0 filas donde antes sumaban ~1.130, verificando las mismas 1.072 claves. **La purga** deja 2.279 filas (primera y última de cada uno de los 1.142 casos) y borró 175.221; ningún otro evento se toca. Es una **excepción explícita al invariante append-only**, que existe para proteger las decisiones humanas y no la telemetría automática: se conservan los dos extremos porque con ellos el historial sigue contestando desde cuándo está verificada una identidad y hasta cuándo se la vio bien. El archivo de la base no se achica sin `VACUUM` —que bloquea la base entera—, así que las páginas quedan libres para reuso en vez de recuperarse. | UM1 / Backend |
| PM-159 | **El programa de plataforma (`2026-09-13-plataforma-auditable-catalogo-identidad-stock-pedidos.md`) pasa a ser la arquitectura y el orden de ejecución del maestro** (decisión del usuario, 2026-09-13). El maestro conserva el *qué*; la plataforma fija el *cómo*: núcleo PostgreSQL único para catálogo, identidad, stock y pedidos, migrado por verticales P0–P6. Reemplaza a UM1 (§18.1) y reordena §19: E23 pasa de última a primera como Gate 0. Motivo medido: Matcher, Identidad y Guardia pisándose; funciones construidas y nunca llamadas (`autoVincularPorSellerSku`, `resolverRetencionPedidoMl`); disco al 91%, Node 20 EOL y DR externo sin funcionar. El usuario no considera un problema el tamaño de la migración. | Programa |
| PM-160 | **Congelamiento del legado durante el programa** (decisión del usuario, 2026-09-13). Sólo arreglos de bugs que pierden plata o bloquean la operación; ninguna función nueva en áreas que una vertical reemplaza. La corrección de conteos con auditoría (spec 2026-09-11) espera al libro de stock; GP13–GP15 van a P4; el alta de taller, garantías y excepciones espera al núcleo nuevo. | Programa |
| PM-161 | **Recepción y conteos entran en el corte de stock (P3)** (decisión del usuario, 2026-09-13). Hoy escriben stock en Woo; dejarlos fuera del corte crea dos escritores remotos, que el programa prohíbe. Hace más grande P3 y se acepta. | P3 |
| PM-162 | **La App iPhone no se rehace sobre v2: `/api/v1` queda como fachada sobre el dominio nuevo y la App migra por OTA** (decisión del usuario, 2026-09-13). Quedan pocas builds de App Store. Verificado en `FusionBikes-App`: `expo-updates` con canales `development/preview/production`, `runtimeVersion` con política `fingerprint` y URL de updates configurada (commit `4084a55` "habilitar OTA"); 23 rutas de `/api/v1` en uso y chequeo de contrato vía `/api/v1/meta`. Se descartó rehacerla ahora porque v2 no existe y la App cubre siete entregas (E5–E7, E13, E15, E17, E21) que dependen de verticales futuras: no se puede asegurar terminarla con 2 builds. La próxima build debe traer todas las capacidades nativas del programa y después se congelan las dependencias nativas. **Pendiente de verificar antes de apostar a esto:** que la build instalada ya incluya `expo-updates` (compilada después de `4084a55`) y que un OTA de prueba en `preview` llegue a un iPhone; publicar requiere autorización. | App |
| PM-163 | **Excepciones físicas, garantías y taller se reconstruyen sobre el núcleo nuevo después de P3**, no se completan en el legado. Medido el 2026-09-12: sus tablas están en 0 filas y ninguna pantalla permite crear el primer caso. No hay datos que migrar. | E18–E20 |
| PM-164 | **Kits/combos entran al programa**, contradiciendo el "fuera de etapa" de §23: la plataforma modela ofertas ML pack/kit con composición versionada. El incidente del GP5000 (2026-09-11, publicación enganchada a un catálogo "Kit De 2 Unidades") fue un kit. | P2–P4 |
| PM-165 | **Sin almacenamiento pago para PostgreSQL** (decisión del usuario, 2026-09-13): no hay tarjeta de la empresa y no se usa una personal. Backblaze B2 queda sólo para los backups SQLite ya existentes dentro de los 10 GB gratis; su tope de 1 GB/día de descarga y el cupo total hacen imposible sostener WAL continuo y un RTO de 1 h desde B2 sin pagar. Se descartan cargar tarjeta y mudar a Cloudflare R2 (sin acceso a la cuenta). | E0 / Operaciones |
| PM-166 | **pgBackRest 2.59.x es la herramienta de WAL, backup y PITR** (decisión del usuario, 2026-09-13). Soporta PostgreSQL 18 desde 2.55.0 (2025-04-21); estuvo archivado del 2026-04-27 al rescate por un consorcio de nueve patrocinadores (mayo 2026) y 2.59.1 salió el 2026-08-17. Aporta archive-push asíncrono con spool local, compresión, cifrado de repositorio, verificación, retención y restauración a un instante. Se descartan scripts Bash propios (reinventan la parte más delicada) y WAL-G (sin soporte PostgreSQL 18 documentado en sus releases al 2026-09-13). Mitigación del bus factor: versión fijada y salida documentada hacia Barman. | E0 / Operaciones |
| PM-167 | **DR de PostgreSQL en dos niveles** (decisión del usuario, 2026-09-13). Nivel 1 — errores de base (migración mala, borrado, corrupción): repositorio pgBackRest cifrado en el mismo VPS con RPO ≤ 5 min y RTO ≤ 1 h. Nivel 2 — pérdida total del VPS: copia externa que **descarga** (pull por SSH de sólo lectura) una máquina propia —Mac o servidor sin uso, a confirmar SO, espacio y disponibilidad— con RPO declarado según esa disponibilidad; así no se abren puertos en el local y un intruso en el VPS no puede borrarla. Red de fondo: backups semanales de Hostinger incluidos en el plan (RPO hasta 7 días). | E0 / Operaciones |
| PM-168 | **Los backups diarios pagos de Hostinger se activan más adelante** (decisión del usuario, 2026-09-13). Mientras no estén activos no forman parte de la aceptación de E0; al activarse bajan el RPO de la red de fondo a 1 día. | E0 / Operaciones |
| PM-169 | **La máquina del nivel 2 de DR es la Mac del local, encendida sólo en horario del local** (decisión del usuario, 2026-09-13). La Mac descarga el repositorio pgBackRest por SSH de sólo lectura mientras está encendida (tarea launchd al iniciar sesión y cada hora). Consecuencia aceptada: ante pérdida total del VPS el RPO es hasta la última sincronización del día hábil —noches, fines de semana y feriados lo alargan— y el RTO incluye copiar el repositorio desde la Mac al servidor de reemplazo. El nivel 1 (RPO ≤ 5 min / RTO ≤ 1 h ante errores de base) no cambia. | E0 / Operaciones |
| PM-170 | **E1 usa `@simplewebauthn/server` 14.x para passkeys** (cierre técnico de decisión abierta de E1, 2026-09-13). 14.0.1 exige Node LTS ≥ 22 y producción corre Node 24.21.0; es TypeScript-first y evita criptografía WebAuthn propia. Versión fijada en `plataforma/package.json`; cambiar de mayor es una decisión revisable. | E1 / Seguridad |
| PM-171 | **El reporte diario de sombra de E1 se envía por el SMTP existente** (cierre técnico, 2026-09-13): el mismo transporte que ya entrega las alertas de incidentes (33 envíos sin error al 2026-09-13), con el reporte JSON y su firma `.sig` adjuntos. No se contrata proveedor de email; el email es aviso, la fuente de verdad es el objeto en B2. | E1 / Operaciones |
| PM-172 | **Manifiestos y reportes firmados de E1 en B2 con Object Lock modo governance y retención de 365 días** (cierre técnico, 2026-09-13; el plazo de un año viene del plan §4.1). Se suben con una clave B2 propia con `writeFiles` y `writeFileRetentions` pero **sin** `deleteFiles` ni `bypassGovernance`: nadie con acceso al VPS puede acortar la retención ni borrar. Los objetos son KB por día, dentro del plan gratuito (PM-165). El dominio TLS de QA no es de E1: pertenece a E4. | E1 / Operaciones |
| PM-173 | **Los servicios de la plataforma (API, worker, scheduler) corren en contenedores Docker separados** (decisión de José, 2026-09-15), con límites de memoria/CPU, `init: true` y la API publicada sólo en `127.0.0.1:3201`; se conectan al PostgreSQL de E0 por la red `fusion-pg_default` con roles sin superusuario. Motivo: aislar la sombra del legado (PM2) y reproducir en `test:e1` los mismos contenedores. | E1 / Operaciones |
| PM-174 | **E1 se entrega en cuatro tramos**: 1 Fundación, 2 Barridos contra simulador, 3 Sombra en vivo (único que toca el legado, con aprobación propia), 4 Passkeys y reporte firmado (decisión de José, 2026-09-15). `test:e1` exige los escenarios del tramo vigente, acumulativos; al cerrar el tramo 4 exige los 24. | E1 |
| PM-175 | **Pila del tramo 1**: Node 24 con TypeScript nativo sin build y `tsc` 7 sólo para tipos, Fastify 5, `pg` con SQL explícito, runner de migraciones propio, Zod 4, Pino 10 y Vitest 5 (decisión de José, 2026-09-15; enfoque A frente a Kysely/node-pg-migrate y Express). Motivo: colas, auditoría encadenada y migraciones seguras en SQL visible y probado. | E1 / Plataforma |
| PM-176 | **Migraciones de la plataforma sólo hacia adelante** con checksum SHA-256 y advisory lock; una migración aplicada alterada o un hueco frenan el arranque; la vuelta atrás es PITR de E0 o migración correctiva (decisión de José, 2026-09-15). Redefine E1-SCH-02. | E1 / Plataforma |
| PM-177 | **pgBackRest sin `archive-push-queue-max`; la protección es capacidad medida** (decisión de José, 2026-09-15, tras revisión técnica). La ayuda de pgBackRest 2.59.1 confirma que al superar el límite informa el WAL como archivado, lo descarta y corta el PITR hasta un backup completo nuevo; la ficha E0 afirmaba lo contrario. Se prioriza el PITR: un disco lleno detiene PostgreSQL. Alertas del vigía: disco del host ≥ 80 % aviso / ≥ 90 % crítico, bytes en cola .ready ≥ 1 GiB / ≥ 4 GiB, pg_wal ≥ 2 GiB / ≥ 8 GiB, medición ausente → crítico. E0 vuelve a `desarrollo` hasta aplicar el cambio, el vigía y la restauración real desde la Mac. | E0 / Operaciones |
| PM-178 | **E1 conserva observaciones y relaciones técnicas sin PII durante 400 días** (decisión de José, 2026-09-15). Las filas activas no expiran; el plazo empieza en el último avistamiento de una fila cerrada o borrada. Los payloads remotos permanecen cifrados y se eliminan a los 90 días. Motivo: permitir conciliación y comparación interanual sin conservar indefinidamente contenido personal. | E1 / Integraciones |
| PM-179 | **T3 separa señales de observaciones autoritativas** (2026-09-16). El webhook sólo crea una señal; `inbox_messages` y `resource_observations` reciben exclusivamente el resultado de una relectura GET remota o un barrido. | E1 T3 |
| PM-180 | **Recibo mínimo de sombra durante 400 días** (decisión de José, 2026-09-16). SQLite conserva canal, cuenta, tópico, recurso, fingerprint, tiempos, estado y razón, nunca el body ni PII. Woo orders persiste ese recibo antes del ACK y devuelve 503 si SQLite no puede hacerlo. | E1 T3 |
| PM-181 | **Las credenciales remotas permanecen en el legado** (2026-09-16). Plataforma usa un gateway interno de operaciones GET tipadas; no recibe tokens ML, claves Woo ni acepta método, host o path arbitrarios. | E1 T3 / Seguridad |
| PM-182 | **T3 cierra con 24 horas en vivo; T4 conserva los siete días contractuales y el reporte firmado** (decisión de José, 2026-09-16). El resumen T3 no se firma ni se envía y no acepta E1 completa. | E1 T3/T4 |
| PM-183 | **Tópicos ML fuera de los ocho de E1 se registran y excluyen** (decisión de José, 2026-09-16). Quedan contados en SQLite con razón, sin copiarse a PostgreSQL ni ampliar implícitamente E1. | E1 T3 |
| PM-184 | **Revisa PM-172: los informes firmados de E1 van a B2 con Object Lock en modo COMPLIANCE, no governance** (decisión de José, 2026-09-17). Governance protege contra alguien que entre al VPS, pero una clave administrativa de la cuenta puede acortar la retención; compliance no. Es irreversible: lo subido queda 365 días. Por eso hay un bucket de verificación aparte y la retención se fija sobre el momento de la subida, no en el contenido firmado. Diseño y plan del tramo 4. | E1 T4 / Operaciones |
| PM-185 | **Ajusta PM-170: `@simplewebauthn/server` fijado en 14.0.2 exacto** (2026-09-17). La 14.0.1 arrastra dos vulnerabilidades moderadas de validación de cadenas de certificados; "14.x" las permitía. Revisión externa del diseño del tramo 4. | E1 T4 / Seguridad |
| PM-186 | **La campaña contractual de 7 días de E1 cuenta sólo días VERDES seguidos** (2026-09-18). El diseño §9 exige cobertura 100 % y convergencia declarada los siete días: un día amarillo (cobertura o convergencia incompleta), uno rojo o uno sin reporte reinician el conteo. Revisión externa de la tanda A del tramo 4. | E1 T4 |

## Pendientes que no deben suponerse

| Pendiente | Responsable | Bloquea | Evidencia de cierre |
| --- | --- | --- | --- |
| **Preguntas, reclamos y mensajes de ML son 100% webhook-driven, sin ningún cron de reconciliación** | Backend (UM1.2 / E6) | Preguntas de clientes que se pierden en silencio | Planteado por el usuario el 2026-09-05: «hay cosas que son solamente webhook driven y deberían tener un cron así sea mínimo». Verificado: **ningún cron reconcilia esas tres entidades contra ML**. `ml_preguntas` (47 filas) y `ml_reclamos` (9) se pueblan sólo por webhook; `ml_mensajes` está en 0 y roto (PM-137). ML **no garantiza entrega ni orden** —el propio documento de arquitectura lo declara y exige reconciliador—, así que un aviso perdido significa una pregunta de cliente que no aparece nunca y de la que nadie se entera. Ya hay evidencia del daño: 3 `question.project` en `dead_lettered` sin nada que los recupere. Comparar con lo que sí tiene red: `items` (scan), `orders` (barrido paginado) y catálogo Woo (`refrescar_catalogo`). Cierre: un cron mínimo y barato por entidad —para preguntas alcanza `/questions/search` filtrando las sin responder, que es el subconjunto que importa— con la misma idempotencia que ya usa la proyección. §15 del plan ya lo pedía («reconciliar contra ML aunque no llegue otro webhook»); esto lo confirma con datos |
| **La proyección de mensajes de ML nunca funcionó: 39 de 39 jobs muertos, y sigue** | Backend + permisos de la app ML (usuario) | Mensajes de clientes que no llegan a la bandeja | `message.project`: **39 dead_lettered, 0 completed** — nunca proyectó uno. No es un episodio cerrado: van del 2026-08-30 al 2026-09-05 (3 hoy mismo). **Causa inmediata**: para el topic `messages` v1, ML manda `resource` como id pelado (`01a07256e97…`, sin barra inicial) y `ingerirMensaje` (`routes/notificacionesMl.js:227`) lo pasa tal cual a `mlFetch`, que lo concatena a la base y produce el host inválido `api.mercadolibre.com01a07256e97…` → `getaddrinfo ENOTFOUND`. ~~**Segunda capa**: el path correcto es `/marketplace/messages/{resource}` … lo que falta es permiso/scope de la app.~~ **CORREGIDO el 2026-09-06 (PM-157): no falta ningún permiso.** `/marketplace/messages` es el recurso de las apps de mensajería de marketplace, no el de un vendedor; por eso responde 403 y por eso ningún scope lo habilita. La vía del vendedor —documentada por ML— es `/messages/unread?role=seller&tag=post_sale` para listar las conversaciones con pendientes y `/messages/packs/{pack}/sellers/{seller}` para leerlas, y **las dos responden 200 con las credenciales actuales**. El id que manda el aviso no es resoluble por ninguna vía de vendedor, así que el webhook sólo puede disparar la reconciliación por pack. |
| **~~El sync ignora escrituras por `user_product_id`~~ → CAUSA REAL: dos publicaciones comparten una bolsa de stock y están mapeadas a productos Woo distintos** | Ventas/Catálogo + Backend | Stock real en ML; hoy hay unidades a la venta sin existencia | 5 publicaciones en bucle desde el 2026-09-01, dos con 34 repeticiones. Patrón cada ~3 h: reconcilia («ML tenía 4, local 0»), empuja 0 a ML, loguea `ok`, y tres horas después ML sigue en 4. Lectura autenticada de los 3 ítems (2026-09-05, solo GET): los tres tienen **`user_product_id`** (`MLAU3210195462`, `MLAU3086754975`, `MLAU211855329`), o sea que ML gestiona su stock a nivel *user product* y un `PUT /items/{id}` con `available_quantity` se acepta con 200 y se ignora. `MLA2472219644` es además `catalog_listing: true` y expone **4 unidades con stock 0 en Woo**. Dos piezas confirman que no podemos ni detectarlo: `buildMlStockUpdate` (`routes/sync.js:203`) sólo conoce `/items/{id}` y `/items/{id}/variations/{varId}`, y **`user_product_id` está NULL en las 6894 filas del cache** —la columna existe desde la migración 082 pero nada la puebla. Cierre: poblar `user_product_id` en el refresco y enrutar el stock de esos ítems al endpoint de user product; hasta entonces, no declarar `ok` una escritura no verificada. Salvedad: `MLA2037451826` hoy muestra en ML la misma cantidad que Woo, así que la hipótesis está fuerte pero no es uniforme en los 3 |
| ~~2 publicaciones ML activas con SKU que no existe en Woo~~ **CERRADO 2026-09-06 (PM-145)** | Ventas/Catálogo | — | `MLA1948065299|` → `FB-67156` (stock ML 3) y `MLA3880051700|` → `FB-68312` (stock ML 1). Ambas `active`, `marketplace`, sin fila en `catalogo_cache`. O el producto se dio de baja en Woo con la publicación viva, o el SKU está mal escrito. Decidir producto Woo destino o excepción `solo_ml` |
| 3 claves ML con SKU exacto pero stock que no coincide con Woo | Integración de stock (E11) | Invariante «stock verificado» de UM1.1 | `MLA2037451826|` = `FB-28334`: Woo 2, ML 1. `MLA2020281886|` y `MLA916195306|81805491379` comparten `FB-2864`: Woo 1 y cada publicación expone 1, o sea **2 unidades expuestas contra 1 real** (riesgo de sobreventa). No es un problema de identidad: la identidad es exacta y el clasificador las marca `stock_no_verificado` correctamente. Se cierran cuando el motor de stock las reconcilie |
| Casos huérfanos por publicación dada de baja en ML | UM1.2 | Limpieza de la cola de trabajo | Detectarlos exige releer el ítem contra ML; hoy el cache puede decir `active` sobre una publicación inexistente. Ejemplo observado: `MLA1764610429` |
| Publicación ML con medidas contradictorias | Ventas | Decisión de identidad de esa clave | `MLA1420680785`: el título dice «123mm», el atributo `AXIS_LENGTH` dice «12.2 cm» y `MODEL` dice «BB-UN300 110 MM». Corregir la publicación en ML |
| Atributos faltantes en 6 variaciones Woo | Ventas/Catálogo | Decisión de identidad en esos casos | `FB-9612`, `FB-9613`, `FB-28297`, `FB-28298`, `FB-28299`, `FB-65471` no tienen `atributos_json`: la medida que las distingue existe sólo en el título de ML. Cargar el atributo en Woo |
| 576 Producto Fusion creados desde padres `variable` | UM1.3 | Invariante «unidad vendible» del plan | Bootstrap que excluya `tipo='variable'` y reconciliación de los ya creados |
| Modelo, driver, lenguaje y puerto de impresora | Depósito | Publicación E3 | Relevamiento físico y prueba de página real |
| Modelo e iOS exactos del iPhone | Equipo móvil | Matriz final E5 | Dispositivo registrado y build probada |
| Campo SLA real por modalidad ML | Integración y operación | Aceptación E1/E4 | Para MercadoEnvíos no es fijo y puede variar por paquete; usar la hora máxima de entrega en centro de acopio del shipment. Flex: salida máxima 17:00. Payload capturado y regla confirmada |
| Infraestructura y sanitización de staging | Operaciones | Pruebas integrales y publicación automática | Instancia aislada sin credenciales de escritura reales |
| RTO y RPO | Operaciones | Aceptación E23 | Medición real de backup y restauración |
