# E1: Apertura, horarios, olas y picking

**Estado:** desarrollo
**Actualizado:** 2026-09-02
**Superficie:** VPS

## Objetivo y requisitos cubiertos

Abrir jornada, confirmar SLA y ejecutar ola inicial congelada con mini-olas continuas.

## Dependencias y bloqueos

E0. Los bloqueos externos y sus responsables se mantienen en el registro de decisiones; no se resuelven por suposición.

## Base, rama y worktree

- Base verificada: `conteo-confiable` en `2fbbfb3 wip(inventario): gate de rollout por SKU en movimientos de stock`.
- Rama: `conteo-confiable` (implementación E1 integrada localmente; no equivale a publicación).
- Worktree: `/opt/fusionbikes/herramientas`.
- Flags/piloto: pendiente de diseñar.

## Estado y evidencia actual

**Implementado en el checkout actual, tests dirigidos verdes, pendiente de gates operativos y cierre.**

Se completaron e implementaron en código:
- Apertura de jornada (routes/jornada.js, lib/jornada.js)
- Ola inicial congelada
- Mini-olas acumulativas
- Mini-ola `ml_urgente` (urgencia diferenciada)
- Claim con freeze atómico y aviso de vencimiento (lib/jornada.js)
- Cierre de jornada

Los tests dirigidos ejecutados sobre el checkout actual pasan **243/243**. La suite global registrada el 2026-09-02 tiene fallos preexistentes reportados en esta ficha; el detalle del supuesto ledger SDD citado históricamente no está presente en este checkout, por lo que no se considera evidencia autónoma suficiente. La revisión independiente de esta versión exacta queda pendiente de evidencia reproducible porque el agente de revisión agotó su cuota. **No se ha ejecutado:** UI, piloto con flag, jornada observada.

Estado: **desarrollo** — no pasa a `candidata` hasta el gate del maestro (§20).

No implica publicación, observación ni aceptación salvo que se documenten abajo con fecha y responsable.

## Commits, diff y migraciones

- Integrados: commits E1 `2dd8ec5`..`8886724`, actualmente alcanzables desde `2fbbfb3`.
- Candidatos: la implementación está integrada localmente; falta gate final sobre el checkout actual.
- Diff pendiente: UI de jornada/ola, picking consolidado por SKU/ruta y piloto operativo.
- Migraciones: `migrations/042_operational_day_waves.sql`, aplicada por `db/index.js` con marcador `operational_day_waves_042`; `ensureTablesJornada` permanece como compatibilidad idempotente.
- Cambios ajenos: `migrations/041_stock_rollout_skus.sql`, `db/index.js`, `routes/inventario.js` y `test/inventario.test.js` forman el trabajo posterior de inventario y no deben atribuirse a E1.

## Gates de revisión y pruebas

- Revisión independiente: pendiente para el checkout actual `2fbbfb3`; la revisión anterior no cubre los commits posteriores de esta rama.
- Críticos/altos: ninguno abierto.
- Tests exactos heredados: `npx vitest run test/jornada.test.js test/preparacion.test.js --reporter=dot` — **243 passed (2 files)**, 153.26 s (2026-09-02).
- Verificación posterior a la migración 042: `npx vitest run test/jornada.test.js --reporter=dot` — **30 passed (1 file)**, 11.02 s (2026-09-02).
- Suite global serial (evidencia heredada, no repetida): `npx vitest run --reporter=dot` — **1913 passed, 5 failed, 1 skipped (1919)**, ~18 min (2026-09-02). Los fallos fueron reportados como preexistentes, pero el ledger SDD citado no está disponible en este checkout; requieren revalidación antes del gate final.
- E2E/dispositivo/hardware: Unit/integración; E2E 390/768/1440; piloto jornada.
- Escenarios mínimos: Pedido nuevo; ML urgente; claim 10/15; hold; multipaquete.
- Auditoría de despliegue/rollback: pendiente.

## Evidencia operativa

- SOP digital/imprimible: pendiente.
- Demo y práctica guiada: pendiente.
- Piloto real por flag: pendiente.
- Jornada observada: pendiente.
- Métricas antes/después: pendiente.
- Aceptación responsable: pendiente.

## Próxima acción reproducible

Despachar revisión independiente sobre `2fbbfb3`; después formalizar migración versionada, definir/pasar la vista operativa de jornada/olas y ejecutar piloto con flag antes de considerar E1 candidata.

## Seguridad y continuidad

No incluir secretos, credenciales, tokens, cookies, datos personales ni volcados de producción. Si se pausa, completar base/rama/worktree, comando siguiente, resultados exactos, estado externo y autoridad faltante.
