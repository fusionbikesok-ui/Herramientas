# E0: Maestro y continuidad documental

**Estado:** candidata
**Actualizado:** 2026-09-01
**Superficie:** Ambos repos

## Objetivo y requisitos cubiertos

Reconstruir fuente canónica, archivo literal, decisiones, patrones, fichas, memoria, coordinación y SOP índice.

## Dependencias y bloqueos

Ninguna. Los bloqueos externos y sus responsables se mantienen en el registro de decisiones; no se resuelven por suposición.

## Base, rama y worktree

- Base verificada: `bc13898f9faeffcde00f49616ce6cb858eff03a3` para la reconstrucción E0; cada entrega futura debe reemplazarla.
- Rama: `docs/plan-maestro-e0-reconstruccion`.
- Worktree: `/tmp/fusion-plan-maestro-e0`.
- Flags/piloto: no aplica a runtime.

## Estado y evidencia actual

Base bc13898; archivo ce5c3cb y maestro reconstruido en worktree aislado. La App `feature/stock-flow-ui` alineó su índice sin duplicar el maestro mediante commits remotos `ac4c48f7b0f602f99578f0949b38ad230733a86c` y `380640f16792e1cb0bb6b3f9b026385e32d5199e`.

No implica publicación, observación ni aceptación salvo que se documenten abajo con fecha y responsable.

## Commits, diff y migraciones

- Integrados en la rama E0: `d1d9cd3`, `76aa031` y `f0f9d05` sobre base `bc13898`.
- Candidatos remotos App ya registrados: `ac4c48f` y `380640f` en `feature/stock-flow-ui`.
- Integración local: fast-forward de `conteo-confiable` a `89c8f7a`; no hay cambios runtime. Push/publicación pendientes de decisión externa.
- Migraciones: ninguna; cambio documental.
- Cambios ajenos preservados: hashes idénticos antes/después para `db/index.js`, `routes/inventario.js`, `test/inventario.test.js` y `migrations/041_stock_rollout_skus.sql`.

## Gates de revisión y pruebas

- Revisión independiente: OK sobre `f0f9d05`; confirmó evidencia E8/E10, dependencias sin ciclos, 25 fichas y referencias correctas.
- Críticos/altos: cero abiertos tras corregir evidencia degradada, ciclos y líneas EOF.
- Tester documental: `git diff --check bc13898..f0f9d05` exit 0; 25/25 fichas; archivo `ce5c3cb` idéntico SHA-256 `cee0f4de6802d2acd4f9add71f06c9d327c7bbe3eb92eb50ed11f5f92daddcb9`; 14 procesos × 15 apartados; seis enlaces WMS HTTP 200; revisión heurística sin secretos/PII nuevos. El revisor estructural verificó contenido y referencias internas; no se atribuyen a él los chequeos del tester.
- Suite global serial: no corresponde a runtime; ejecutar validaciones documentales.
- E2E/dispositivo/hardware: Cobertura documental; enlaces/rutas; ausencia de contradicciones y secretos.
- Escenarios mínimos: Plan histórico recuperable; agente nuevo reproduce próxima acción.
- Auditoría final: 🟢 sobre `89c8f7a`; diff exclusivamente documental, rama lineal, seguridad heurística sin hallazgos y runtime/UI/esquema/migraciones no aplicables. No hubo deploy.

## Evidencia operativa

- SOP: índice y plantilla creados; cada SOP de proceso se redacta y valida en su entrega.
- Demo y práctica guiada: pendiente.
- Piloto real por flag: pendiente.
- Jornada observada: pendiente.
- Métricas antes/después: pendiente.
- Aceptación responsable: pendiente.

## Próxima acción reproducible

Completar sincronización de memoria/coordination, ejecutar validaciones documentales, revisión independiente y auditoría; integrar sin tocar cambios de inventario del checkout principal.

## Seguridad y continuidad

No incluir secretos, credenciales, tokens, cookies, datos personales ni volcados de producción. Si se pausa, completar base/rama/worktree, comando siguiente, resultados exactos, estado externo y autoridad faltante.
