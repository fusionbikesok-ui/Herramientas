# E3: Etiqueta interna 50×25

**Estado:** desarrollo
**Actualizado:** 2026-09-03
**Superficie:** VPS + Windows + USB

## Objetivo y requisitos cubiertos

Imprimir automáticamente un job idempotente por paquete aprobado.

## Dependencias y bloqueos

E2 y relevamiento impresora. Los bloqueos externos y sus responsables se mantienen en el registro de decisiones; no se resuelven por suposición.

## Base, rama y worktree

- Base verificada: `bc13898f9faeffcde00f49616ce6cb858eff03a3` para la reconstrucción E0; cada entrega futura debe reemplazarla.
- Rama: pendiente de crear.
- Worktree: pendiente.
- Flags/piloto: pendiente de diseñar.

## Estado y evidencia actual

Cola/agente candidatos existen; modelo/driver/lenguaje/puerto bloquean publicación.

El montaje del servidor ahora acepta Bearer JWT revocable de dispositivo para el agente en `/api/etiquetas`, manteniendo sesión web para el panel; el permiso `etiquetas` continúa aplicado por el guard de alcance. Los resultados de impresión son idempotentes ante reenvío con el último claim. La validación local dirigida actual queda en **37/37** (14 omitidas por filtro), con `test/etiquetas.test.js` en **14/14** y la integración Bearer mediante `buildApp` en `test/server.test.js` en **15/15** (2026-09-03). Falta probar permisos configurados en dispositivo operativo y hardware real.

Validación local del backend de cola: `npx vitest run test/etiquetas.test.js test/cobertura-cola.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **36/36 aprobadas** (2026-09-03). Esta evidencia no cubre la PC Windows ni la impresora térmica real.

No implica publicación, observación ni aceptación salvo que se documenten abajo con fecha y responsable.

## Commits, diff y migraciones

- Integrados: pendiente de registrar.
- Candidatos: revisar ancestry y diff; no hacer merge ciego.
- Diff pendiente: pendiente.
- Migración candidata: `/opt/fusionbikes/herramientas/migrations/049_etiquetas_agente_claim_resultado.sql`; el runner la aplica de forma aditiva/idempotente y registra `etiquetas_agente_claim_resultado_049`. Revisión de migración en producción pendiente.
- Cambios ajenos preservados: registrar antes/después.

## Gates de revisión y pruebas

- Revisión independiente: pendiente.
- Críticos/altos: pendiente.
- Tests exactos: `npx vitest run test/db.test.js test/etiquetas.test.js test/server.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **38/38 aprobados** (2026-09-03); `node --check db/index.js`; validación Windows/impresora pendiente.
- Autenticación del agente: `npx vitest run test/server.test.js -t "agente de etiquetas" --no-file-parallelism --testTimeout=30000 --reporter=dot` — **1/1 aprobado**; verifica JWT de dispositivo con permiso `etiquetas` y rechazo 403 sin ese permiso (2026-09-03).
- Verificación dirigida final: `npx vitest run test/etiquetas.test.js test/server.test.js -t "agente|reclama|lease" --no-file-parallelism --testTimeout=30000 --reporter=dot` — **3/3 aprobadas** (2026-09-03); `server.js` y `routes/etiquetas.js` pasan `node --check`.
- Regresión dirigida ampliada: `npx vitest run test/etiquetas.test.js test/cobertura-cola.test.js test/server.test.js -t "agente|reclama|lease|impres|cola" --no-file-parallelism --testTimeout=30000 --reporter=dot` — **37 aprobadas, 14 omitidas** (2026-09-03). Confirma cola, claim/lease, resultado idempotente, reintento y permiso Bearer del agente.
- Suite global serial: pendiente.
- E2E/dispositivo/hardware: `--once` permite diagnóstico controlado del agente; falta ejecutar en Windows con impresora real, además de reinicio/rollback.
- Escenarios mínimos: Apagada; sin papel; USB/red/Windows; respuesta perdida; reimpresión.
- Auditoría de despliegue/rollback: pendiente.

## Evidencia operativa

- SOP digital/imprimible: pendiente.
- Demo y práctica guiada: pendiente.
- Piloto real por flag: pendiente.
- Jornada observada: pendiente.
- Métricas antes/después: pendiente.
- Aceptación responsable: pendiente.

## Próxima acción reproducible

Abrir esta ficha junto con el maestro, confirmar el permiso del dispositivo de prueba, relevar modelo/driver/lenguaje/puerto de la impresora del depósito y ejecutar el checklist de hardware; no instalar el agente ni publicar hasta contar con esos datos y autorización explícita.

## Seguridad y continuidad

No incluir secretos, credenciales, tokens, cookies, datos personales ni volcados de producción. Si se pausa, completar base/rama/worktree, comando siguiente, resultados exactos, estado externo y autoridad faltante.
