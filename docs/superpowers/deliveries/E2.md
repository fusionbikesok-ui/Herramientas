# E2: Evidencia, perfiles, paquetes y aprobación

**Estado:** desarrollo

**Evidencia reciente:** `npm run e2e:e2` — carga multipart, idempotencia, recarga, timeout/reintento y respuesta tardía reconciliada OK. Aún no implica aceptación: falta hardware real y observación operativa.
**Actualizado:** 2026-09-03
**Superficie:** VPS + web móvil

## Objetivo y requisitos cubiertos

Hacer fotos recuperables y aprobación atómica por paquete con perfiles versionados.

## Dependencias y bloqueos

E1. Los bloqueos externos y sus responsables se mantienen en el registro de decisiones; no se resuelven por suposición.

## Base, rama y worktree

- Base verificada: `bc13898f9faeffcde00f49616ce6cb858eff03a3` para la reconstrucción E0; cada entrega futura debe reemplazarla.
- Rama: pendiente de crear.
- Worktree: pendiente.
- Flags/piloto: pendiente de diseñar.

## Estado y evidencia actual

Existe base funcional de fotos/evidencia y perfiles por categoría/SKU; no hay aceptación integral.

Verificado en el checkout actual:
- Requisitos de evidencia por perfil (`bici`, `kit_transmision`, `sellado`) y estado de embalaje.
- Overrides por SKU y categoría, con perfil seguro por defecto.
- Aprobación rechazada si faltan ítems, fotos o evidencia de paquete.
- Subida multipart idempotente mediante `upload_id` y reintento de fotos en error.
- Test dirigido: `npx vitest run test/preparacion.test.js -t "perfiles|requisitosFoto|completar exige|subir foto es idempotente" --no-file-parallelism --testTimeout=30000` — **10 passed, 208 skipped** (2026-09-02).
- Versionado de reglas: migración 045 y `preparacion_items.perfil_version`; los overrides de categoría/SKU incrementan su versión al modificarse y la versión aplicada queda registrada al crear el ítem. Verificado: **13 tests de perfiles + 9 tests de esquema aprobados** (2026-09-02).
- Congelamiento de requisitos: migración 046 y `preparacion_items.requisitos_json_snapshot`; las fotos se validan contra los requisitos capturados al crear el ítem, aunque la regla vigente cambie después. Verificado junto con los tests de perfiles y esquema (2026-09-02).
- El snapshot conserva los requisitos por estado de embalaje (`default` y `re_embalada`): cambiar una bici a re-embalada activa `lado_a`, `lado_b` y `caja_accesorios` sin volver a leer reglas de catálogo modificadas. Regresión cubierta: **1 aprobado** (2026-09-02).
- Retención: migración 047, purga normal a 180 días y tabla `preparacion_fotos_holds`; una preparación con hold activo no se purga. Tests de purga: **3 aprobados** y tests de esquema: **9 aprobados**; ciclo de autorización/validación/actualización/eliminación de hold: **1 aprobado** (2026-09-02).
- E2E aislado reproducible: `npm run e2e:e2` — **upload multipart + idempotencia + persistencia tras recarga + timeout abortado con reintento visual + respuesta tardía reconciliada sin duplicado OK** (2026-09-03), usando base SQLite temporal, sesión efímera y navegador Playwright.
- Smoke de navegador base `node scripts/e1-browser-smoke.mjs`: axe y recorrido autenticado de preparación aprobados en **390×844, 768×900 y 1440×900**, sin violaciones críticas/serias (2026-09-03). Este resultado no sustituye el piloto móvil de E2.

Brechas pendientes de E2: piloto móvil web y revisión integral de la operación. La recarga, idempotencia, timeout abortado, respuesta tardía con persistencia, reconciliación sin duplicado, versión de perfil, retención con holds y estado visual de reintento ya tienen implementación y pruebas dirigidas.

No implica publicación, observación ni aceptación salvo que se documenten abajo con fecha y responsable.

## Commits, diff y migraciones

- Integrados: pendiente de registrar.
- Candidatos: revisar ancestry y diff; no hacer merge ciego.
- Diff pendiente: pendiente.
- Migraciones: 045, 046 y 047 aplicadas mediante runner idempotente; los SQL documentan que deben ejecutarse por `openDb()` (SQLite no ofrece `ADD COLUMN IF NOT EXISTS`). Falta cerrar la revisión independiente en el gate de E2.
- Cambios ajenos preservados: registrar antes/después.

## Gates de revisión y pruebas

- Revisión independiente: pendiente; checklist reproducible en `docs/superpowers/deliveries/E2-review-checklist.md`.
- Críticos/altos: pendiente.
- Tests exactos: `npx vitest run test/preparacion.test.js test/preparacion-render.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **244/244 aprobados** (135.91 s; 2026-09-03); E2E `npm run e2e:e2` aprobado. La suite cubre idempotencia compatible, conflicto de fingerprint, transacción foto+auditoría, borrado protegido tras aprobación, retención y recuperación de cola por lease. Piloto y revisión independiente siguen pendientes.
- Suite global serial: pendiente.
- E2E/dispositivo/hardware: Uploads idempotentes y smoke autenticado/axe ejecutados nuevamente el 2026-09-03 en 390×844, 768×900 y 1440×900, con 0 violaciones axe. La prueba sigue siendo de navegador web; falta piloto en el dispositivo operativo.
- Escenarios mínimos: Lenta; timeout; tardía; doble toque; recarga; perfil ausente.
- Auditoría de despliegue/rollback: pendiente.

## Evidencia operativa

### Validación reciente (2026-09-03)

- Suite E2 de preparación y render: `npx vitest run test/preparacion.test.js test/preparacion-render.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **244/244** (2026-09-03).
- Regresión conjunta posterior a los cambios de E1/E2: `npx vitest run test/jornada.test.js test/preparacion.test.js test/preparacion-render.test.js test/fotos-preparacion-cola.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **296/296** (4 archivos, 2026-09-03).
- La corrida inicialmente pareció fallar por SQLite de solo lectura; la causa fue un proceso anterior todavía vivo usando el mismo fixture. Tras finalizarlo y aislar los artefactos, la corrida completa pasó.

- SOP digital/imprimible: `docs/operations/sops/PILOTO-E2-fotos-checklist.md` preparado; no ejecutado.
- Demo y práctica guiada: pendiente.
- Piloto real por flag: pendiente de autorización, selección y entorno aislado.
- Jornada observada: pendiente.
- Métricas antes/después: pendiente.
- Aceptación responsable: pendiente.

## Próxima acción reproducible

Ejecutar la suite integral serial, completar autorización/entorno del checklist de piloto web móvil y solicitar revisión independiente antes de cualquier prueba real.

## Seguridad y continuidad

No incluir secretos, credenciales, tokens, cookies, datos personales ni volcados de producción. Si se pausa, completar base/rama/worktree, comando siguiente, resultados exactos, estado externo y autoridad faltante.
