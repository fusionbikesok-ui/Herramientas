# E21: Taller iPhone y offline

**Estado:** planificada
**Superficie:** App iPhone

## Estado verificado

La API VPS de taller expone trabajos, diagnóstico/presupuesto, repuestos,
consumo/devolución contable y outbox Woo. La App no está disponible localmente en
`/opt/fusionbikes/FusionBikes-App`; el clon del repositorio privado falló por falta de
autenticación de GitHub (`could not read Username`). No se modificó ningún repositorio móvil.

## Alcance previsto

Consulta y ejecución de trabajos de taller desde iPhone, captura provisional offline,
sincronización ordenada, conflictos explícitos y fotos/notas según el contrato `/api/v1`.
La web mantiene el control y la contingencia.

## Bloqueo reproducible

`git clone https://github.com/fusionbikesok-ui/FusionBikes-App.git /opt/fusionbikes/FusionBikes-App`
requiere autenticación. Resolver con acceso autorizado o un checkout proporcionado por el
equipo móvil; luego verificar rama objetivo, contrato y cambios locales antes de implementar.

## Criterio de inicio

Repositorio accesible, rama `feature/stock-flow-ui` verificada, modelo de iPhone confirmado,
contrato `/api/v1` fijado por commit y prueba de autenticación contra un entorno aislado.
