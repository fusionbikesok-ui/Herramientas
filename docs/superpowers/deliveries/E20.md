# E20: Taller web y Woo

**Estado:** desarrollo
**Actualizado:** 2026-09-03
**Superficie:** VPS

## Objetivo y requisitos cubiertos

Gestionar trabajos, aprobación, repuestos y venta Woo desde web.

## Dependencias y bloqueos

E10–E12, E19. Los bloqueos externos y sus responsables se mantienen en el registro de decisiones; no se resuelven por suposición.

## Base, rama y worktree

- Base verificada: `bc13898f9faeffcde00f49616ce6cb858eff03a3` para la reconstrucción E0; cada entrega futura debe reemplazarla.
- Rama: pendiente de crear.
- Worktree: pendiente.
- Flags/piloto: pendiente de diseñar.

## Estado y evidencia actual

Existe un núcleo durable: `workshop_jobs`, `workshop_events` y `workshop_parts`, con alta
idempotente, estados versionados, diagnóstico/presupuesto, repuestos comprometidos/instalados/
devueltos y timeline mediante `/api/workshop`. La bandeja `/taller/` ya ofrece consulta rápida
de trabajos y presupuestos. Todavía faltan tarifas/checklists verificadas, ledger de repuestos,
venta Woo y piloto. La bandeja ya permite cargar diagnóstico/presupuesto y avanzar estados con versión esperada e idempotencia. E20 añade ahora consumo de repuesto como salida contable desde ubicación, enlazado al repuesto y protegido por idempotencia; la devolución se implementará como entrada inversa.

No implica publicación, observación ni aceptación salvo que se documenten abajo con fecha y responsable.

## Commits, diff y migraciones

- Integrados: pendiente de registrar.
- Candidatos: revisar ancestry y diff; no hacer merge ciego.
- Diff pendiente: pendiente.
- Migraciones: pendiente de diseño y revisión aditiva/idempotente.
- Cambios ajenos preservados: registrar antes/después.

## Gates de revisión y pruebas

- Revisión independiente: pendiente.
- Críticos/altos: pendiente.
- Tests exactos: `npx vitest run test/workshop.test.js test/db.test.js test/server.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **26/26 aprobadas** (2026-09-03).
- E2E aislada: `npm run e2e:e20` — **OK**, bandeja y acciones visibles (2026-09-03).
- Stock de taller: `npx vitest run test/workshop-stock.test.js test/workshop.test.js test/db.test.js --no-file-parallelism --testTimeout=30000 --reporter=dot` — **12/12 aprobadas** (2026-09-03).
- Suite global serial: pendiente.
- E2E/dispositivo/hardware: Woo; movimientos; E2E responsive; piloto.
- Escenarios mínimos: Presupuesto; usado/no usado; saldo; checklist.
- Auditoría de despliegue/rollback: pendiente.

## Evidencia operativa

- SOP digital/imprimible: pendiente.
- Demo y práctica guiada: pendiente.
- Piloto real por flag: pendiente.
- Jornada observada: pendiente.
- Métricas antes/después: pendiente.
- Aceptación responsable: pendiente.

## Próxima acción reproducible

Relevar tarifas/checklists con el taller, completar devolución inversa y venta Woo sin duplicar movimientos; después ampliar E2E de acciones y piloto antes de solicitar `candidata`.

## Seguridad y continuidad

No incluir secretos, credenciales, tokens, cookies, datos personales ni volcados de producción. Si se pausa, completar base/rama/worktree, comando siguiente, resultados exactos, estado externo y autoridad faltante.
