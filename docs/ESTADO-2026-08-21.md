# Estado al 2026-08-21 — rama `conteo-confiable-revisado`

> **Histórico, no vigente.** Esta fotografía se conserva literalmente salvo esta advertencia de
> clasificación. Para estado actual usar `/opt/fusionbikes/herramientas/docs/memory/active.md`; para
> especificación y progreso E0–E24 usar `/opt/fusionbikes/herramientas/docs/superpowers/INDEX.md`.

Documento de continuidad: qué está hecho, qué falta y qué decisiones ya están tomadas.
Escrito para que cualquiera (o yo en otra sesión) pueda retomar sin releer la conversación.

**Worktree:** `/opt/fusionbikes/herramientas/.claude/worktrees/matcher-unificado`
**Rama:** `conteo-confiable-revisado` · **base de integración:** `conteo-confiable` (`19e1f9e`)
La integración y el reinicio de `pm2 herramientas` fueron ejecutados por el orquestador
autorizado: merge `6cd89e7`, publicación en GitHub y PM2 online tras reinicio. El endpoint
local `/login/` respondió 200.

## Actualización de continuidad — 2026-08-22

Esta sección reemplaza el estado de revisión de los puntos 4 y 5 cuando haya contradicción.

- La rama de integración operativa es `conteo-confiable`, actualmente en `19e1f9e`; esta rama
  contiene los commits de Entrega 2 más el WIP pendiente de commit.
- H6 quedó resuelto con una única función `pendientesDeSesion`, compartida por detalle,
  cierres en cero y gate de confirmación.
- H11 quedó resuelto reutilizando los candidatos precalculados de `cerrar-sin-stock`.
- H14 quedó cubierto para un SKU `sin_stock` y para un SKU del snapshot de otra sesión.
- `/cerrar-en-cero` rechaza siempre `todos:true`, incluso si llega junto con `skus`.
- La UI ya no cierra Revisión cuando guardar los ceros responde `{ok:false}` y un escaneo
  con respuesta perdida se reconcilia; si tampoco puede leer la sesión, bloquea el scanner.
- El contrato C1 documenta `motivo='woo'|'sku_ambiguo'|'no_endpoint'`.
- Pruebas dirigidas frescas: inventario 129/129, códigos 15/15 y wooStock 11/11; diff-check verde.

- El bloqueo de foco del conflicto GTIN quedó corregido: se cierra la hoja antes de abrir el
  diálogo, se restaura solo a un elemento conectado y visible, y se usa `scanInput` como
  fallback. El control Reintentar mantiene el scanner bloqueado hasta recuperar la sesión.

Handoff puntual: `/tmp/fusion-handoff-2026-08-22.md`. Protocolo durable:
`docs/agent-coordination.md` en el checkout principal.

---

## 1. URGENTE Y ABIERTO — stock publicado sin verificar

Ver `docs/incidentes/2026-08-21-sobreventa-por-no-contado.md` y la lista
`docs/incidentes/2026-08-21-stock-sin-verificar.csv` (ya entregada al usuario).

**13 productos / 17 unidades** siguen publicados sin que nadie los haya verificado
físicamente, de las sesiones **8** y **10**. Los 9 de la sesión 14 ya se pusieron en 0 en Woo
a mano el 2026-08-21 ~13:38.

**Decisión del usuario:** se revisan a mano en el depósito con esa lista. **No** se ajusta
nada automáticamente.

Los más expuestos: FB-60452 (3 u.), FB-25295 (2 u.), FB-31760 (2 u.).

---

## 2. Entregas terminadas en la rama (sin desplegar)

### A · El `+1` del escaneo ya no se pierde
El bug tuvo **tres capas**, cada una tapando a la siguiente (detalle en
`docs/superpowers/specs/2026-08-20-conteo-confiable-design.md`, sección "Revisión del
2026-08-21"):
1. dos escrituras con semánticas distintas (escaneo `+1` relativo vs PATCH absoluto) → cola
   que las serializa;
2. el `−` fijaba su valor al hacer clic, no al ejecutarse → pasa una **función** que se
   resuelve cuando le toca el turno;
3. un `cantidadSeq` sobrante descartaba la respuesta **buena** y dejaba un valor optimista
   obsoleto como base → eliminado.

Verificado en navegador contra la base: 3 pares escaneo+`−` sobre cantidad 4 terminan en **4**
(antes 2), en desktop y en 390×844. Más: borrar durante un escaneo en vuelo ya no resucita la
fila, el foco no se roba mientras se tipea una cantidad, `-3` guarda 0 avisando, y un escaneo
perdido por red ya no falla en silencio.

### B · Cierre seguro de sesión
Spec: `docs/superpowers/specs/2026-08-21-cierre-seguro-conteo-design.md`
Plan vigente: `docs/superpowers/plans/plan-maestro-v2.md`

- Gate **409** en `/confirmar` si quedan pendientes del bloque `con_stock`. Va **antes** del
  reclamo atómico y corre **solo con la sesión abierta**.
- `POST /sesiones/:id/cerrar-en-cero`, **solo por lista explícita, nunca `todos:true`**.
- Pantalla de cierre `#cierre-veil`: casilla "no lo revisé" por producto; sin tildar = pasa a
  0 (default deliberado); "Pasar a 0 y confirmar" habilitado solo si no hay ninguna tildada;
  "Guardar lo decidido y seguir después" deja la sesión **abierta**.

### C1 · Subir a Woo el código escaneado
Spec: `docs/superpowers/specs/2026-08-21-subir-ean-a-woo-design.md`

- `lib/gtinWoo.js` nuevo (extraído de `routes/codigos.js`, misma semántica verificada).
- `/asociar` sube el código como `global_unique_id` si es un GTIN válido (`looksLikeEan`:
  8/12/13/14 dígitos + dígito de control GS1).
- Estados: `subido | sin_cambio | conflicto | no_valido | fallo` (+ `motivo`).
- Conflicto (el producto ya tiene otro código) → **no se pisa**, la pantalla pregunta.
- Si Woo rechaza → el conteo **no se pierde**, se avisa.

---

## 3. Decisiones del usuario (no volver a preguntar)

| Tema | Decisión |
|---|---|
| Confirmar con productos sin contar | **No se puede.** Hay que decidir uno por uno |
| Los "no lo revisé" | **No pueden quedar al aire**: la sesión queda **abierta** con esos pendientes |
| Botón masivo de "no revisé" | Solo selección explícita de cuáles no revisó |
| Lo ya expuesto (17 u.) | Revisar **a mano** con la lista. No ajustar automáticamente |
| Código ya cargado distinto en Woo | **Preguntar en pantalla cada vez** (ni pisar ni saltear en silencio) |
| Woo rechaza el código | **El conteo cuenta igual**, con aviso fuerte |
| Dónde subir el código | Contador (C1, hecho), Consulta de Precios (C2), Preparación (C3) |

---

### Actualización de continuidad — 2026-08-22 (segunda)

- Corregido el orden del conflicto: la hoja EAN se cierra sin enfocar el scanner antes de abrir
  el diálogo; el refresco de conteo no roba el foco cuando hay una veil abierta.
- Agregada cobertura de contrato para la secuencia y el guard de foco.
- Suites dirigidas: inventario **129/129**, códigos **15/15** y wooStock **11/11** en verde.
- Suite global con `--testTimeout=60000 --reporter=dot`: **65 archivos, 1326 tests en verde, 1 skip**.
- Estado: **INTEGRADO_LOCAL**, revisión formal nueva y auditoría Claude pendientes por
  indisponibilidad temporal del ejecutable; los hallazgos de alta prioridad fueron corregidos,
  verificados localmente y el E2E previo quedó aprobado.

## 4. LO QUE FALTA

### Ciclo de control de A + B + C1 (bloquea el despliegue)
1. **Re-revisión.** El `revisor` dio **NO aprobado** sobre `d7682b4..HEAD`; los hallazgos
   bloqueantes e importantes fueron corregidos en el WIP y verificados con pruebas dirigidas.
   Falta registrar un handoff formal nuevo cuando el proceso Claude vuelva a responder.
2. **`probador-e2e`** sobre la pantalla de cierre y el modal de conflicto de código con datos
   reales (el frontend los probó con `fetch` mockeado, no end-to-end).
3. **`auditor-despliegue`**, con el veredicto final del revisor y el reporte del e2e pegados
   en el prompt. Sin esos dos insumos es 🔴 automático.

### Hallazgos corregidos en esta pasada
- La definición de pendientes se comparte entre detalle, cierre en cero y gate de confirmación.
- El control de reintento es fijo, visible y enfoca su botón; el scanner queda bloqueado hasta
  reconciliar.
- Las respuestas `ok:false` de sesión se distinguen de errores de red y vuelven a Inicio con el
  mensaje del servidor.
- `/confirmar` rechaza SKU homónimos antes de realizar cualquier escritura en Woo.
- Los canarios frontend están identificados como canarios de contrato; el comportamiento real se
  cubre en E2E.

### Entregas planeadas y no empezadas
- **C2** — subir el EAN a Woo desde **Consulta de Precios** (tarea #25). Verificar primero si
  esa pantalla ya tiene flujo de "enseñar" el EAN o hay que crearlo (`routes/consultaPrecios.js`
  tiene `pareceEan` en `:18`).
- **C3** — ídem en **Preparación de pedidos** (tarea #26). Verificar primero qué hace hoy con un
  código desconocido; puede que interrumpa demasiado el armado del pedido — a conversar.
- **E3** — detectar publicaciones nuevas de ML sin inundar la API (#16) y ponerlas primero en
  la cola del Matcher + aviso en Home (#17). `start_time_desc` **sí existe** en la API de ML
  (verificado), lo que desbloquea hacerlo con 1-3 llamadas en vez de 273.
- **#22** — republicar en ML las 3 cerradas que tienen stock en la web (lo hace el usuario).

---

## 5. Trampas de este repo (caras de re-aprender)

- **`npm test` devuelve exit code 0 aunque haya archivos en rojo.** Hay que leer el resumen
  "Test Files".
- **La suite completa no convive con nada.** Ni otra corrida ni un servidor de `probador-e2e`.
  Produce fallos con forma de aserción real, indistinguibles de una regresión.

- **Y además tiene fallos intermitentes bajo carga, aun corriendo sola.** El 2026-08-21 se
  corrió **tres veces** y dio **1 archivo en rojo cada vez, y un archivo DISTINTO cada vez**:

  | Corrida | Archivo en rojo | Corrido solo |
  |---|---|---|
  | 1ª | `matcherPush.test.js` | **28/28 verde** |
  | 2ª | `vinculos-route.test.js` | **26/26 verde** |
  | 3ª | `reactivar-automatico.test.js` | **27/27 verde** |

  Ninguno de los tres toca el Contador de inventario. Los tres tienen aserciones sensibles al
  tiempo real (cooldowns, `CALL_DELAY_MS`, ventanas de corrida) y la suite entera tarda ~515 s,
  así que bajo carga alguno se pasa de su ventana. **1313 tests pasan, 1 skip.**

  Consecuencia práctica: **un solo archivo en rojo en la suite completa no es evidencia de
  regresión.** Hay que re-correr ese archivo solo antes de creerle — y quien audite tiene que
  saberlo de antemano o va a reportar un 🔴 falso. Vale la pena atacar la causa (tests atados a
  tiempo real → relojes falsos) en una entrega propia.
- **Nunca escribir en `data/*.sqlite`.** Los agentes usan copias, con `DISABLE_CRONS=true`,
  puerto libre (nunca 3001) y **`DELETE FROM ml_oauth_token` en la copia** — las variables de
  entorno no alcanzan; hubo un incidente de escritura real en ML producción.
- **El índice de `sku` en `catalogo_cache` NO es único.** Ya hubo 3 productos de WC con el
  mismo SKU (incidente 2026-07-25).
- `codigos` e `inventario` son **permisos distintos** (`lib/permisos.js`): un operario de
  conteo no puede llamar a `/api/codigos/*`.
- El proceso de pm2 se llama **`herramientas`**, no `herramientas-app`.
