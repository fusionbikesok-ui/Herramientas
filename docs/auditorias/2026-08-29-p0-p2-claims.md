# Auditoría P0.1–P2 Claims — 2026-08-29

## Alcance

Trabajo validado en la integración aislada `/tmp/fusion-hito7-integrated`, rama
`hito7-integrated`, basada en `conteo-confiable` (`f0ef658`). Producción, despliegues y el
worktree de Hito 7 no se modificaron.

## Evidencia actual

- `npm run verify:agents`: configuración de agentes válida (9 roles).
- Suite dirigida Claims/móvil/push: 11 archivos, 122/122 tests verdes.
- Suite global serial (`npx vitest run --testTimeout=60000 --reporter=dot --maxWorkers=1 --no-file-parallelism`):
  87 archivos pasan, 1800 tests pasan, 1 skip. No quedan fallos de assertions.
- Regresión de migración: `db+claims+mobileAuth` 21/21 tests verdes; Claims repara tablas
  ausentes aunque `user_version` ya sea 30 y conserva esa versión.
- E2E aislado con SQLite sintética, `DISABLE_CRONS=true`, servidor local en puerto 3198:
  login y Home verificados; endpoints Home respondieron 200; capturas desktop/tablet/móvil;
  viewport móvil 390 px sin overflow; consola sin errores ni warnings. Servidor, navegador y DB
  temporal fueron cerrados/eliminados.

## Estado de integración

- El endpoint `/diferencias/:id/aprobar` exige nuevamente administrador y el historial excluye
  sesiones abiertas.
- La migración Hito 7 queda renumerada a 030; su SQL es referencia DDL y la vía soportada es
  el runner transaccional de `db/index.js`.
- E2E Home aislado: login, Home desktop/tablet/móvil, viewport 390 px sin overflow, consola sin
  errores ni warnings y endpoints Home 200.
- El merge a producción queda deliberadamente pendiente de la auditoría final; no hay deploy.
