# Auditoría P0.1–P2 Claims — 2026-08-29

## Alcance

Worktree aislado `/tmp/fusion-p0-claims`, rama `p0-claims-real`. No se modificaron producción,
despliegues ni la línea de Hito 7.

## Evidencia actual

- `npm run verify:agents`: configuración de agentes válida (9 roles).
- Suite dirigida Claims/móvil/push: 4 archivos, 7 tests, todos verdes.
- Suite global serial (`npx vitest run --testTimeout=60000 --reporter=dot --maxWorkers=1 --no-file-parallelism`):
  86 archivos pasan, 1762 tests pasan, 1 skip; quedan 2 fallos preexistentes de Inventario.
  No hubo fallos en Claims, backbone, inbox, API móvil ni worker durable.
- E2E aislado con SQLite sintética, `DISABLE_CRONS=true`, servidor local en puerto 3198:
  login y Home verificados; endpoints Home respondieron 200; capturas desktop/tablet/móvil;
  viewport móvil 390 px sin overflow; consola sin errores ni warnings. Servidor, navegador y DB
  temporal fueron cerrados/eliminados.

## Hallazgos no bloqueantes del alcance

1. `test/inventario.test.js` espera ocultar sesiones abiertas, mientras la ruta actual las incluye.
2. `test/inventario.test.js` espera 403 para aprobar una diferencia sin admin, mientras el código
   conserva un bypass temporal explícito. Aplicar ese cambio sin resolver el contrato rompe otros
   tests de Inventario; queda fuera de Claims.

## Pendientes que impiden declarar P2 completo

- No existe aún un cliente móvil Expo/TypeScript en este repositorio.
- El gate de permiso `notificaciones-ml` ya está aplicado a las APIs móviles de Claims; queda
  definir la resolución automática por equipo/rol para crear destinatarios sin asignación manual.
- Falta proveedor push real detrás de feature flag y validación en dispositivo real.
- Falta auditor/revisor formal con luz verde sobre la base final.
- La integración a `master`/`conteo-confiable` no está autorizada ni ejecutada.
